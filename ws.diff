diff --git a/lute/cli/commands/pkg/loom-core/src/commands/install.luau b/lute/cli/commands/pkg/loom-core/src/commands/install.luau
index 6e0f3123..3f075a32 100644
--- a/lute/cli/commands/pkg/loom-core/src/commands/install.luau
+++ b/lute/cli/commands/pkg/loom-core/src/commands/install.luau
@@ -1,9 +1,9 @@
 local fs = require("@std/fs")
 local lockfile = require("../lockfile")
 local manifest = require("../manifest")
-local resolution = require("../resolution")
 local path = require("@std/path")
 local process = require("@std/process")
+local resolution = require("../resolution")
 
 local function printDiagnostics(lock: lockfile.Lockfile)
 	local depCount = 0
@@ -15,10 +15,8 @@ local function printDiagnostics(lock: lockfile.Lockfile)
 	for _, key in lock.dependencies do
 		local transitive = 0
 		local pkg = lock.packages[key]
-		if pkg then
-			for _ in pkg.dependencies do
-				transitive += 1
-			end
+		for _ in pkg.dependencies do
+			transitive += 1
 		end
 		print(`  {key} ({pkg.sourceKind}, {transitive} dep(s))`)
 	end
@@ -29,7 +27,7 @@ local function install()
 	local manifestPath = `{rootDirectory}/loom.config.luau`
 
 	if fs.exists(manifestPath) then
-		local rootManifest = manifest.parseFile(manifestPath)
+		local rootManifest = manifest.parseManifest(rootDirectory)
 		for _, depSpec in rootManifest.package.dependencies do
 			if depSpec.sourceKind == "registry" then
 				error("Registry dependencies are not yet supported by the install command")
@@ -39,6 +37,7 @@ local function install()
 
 	local lock = resolution.resolve(rootDirectory, nil)
 	printDiagnostics(lock)
+	lockfile.writeLockfile(lock, rootDirectory)
 end
 
 return install
diff --git a/lute/cli/commands/pkg/loom-core/src/lockfile.luau b/lute/cli/commands/pkg/loom-core/src/lockfile.luau
index 8cdd3c22..8d61d381 100644
--- a/lute/cli/commands/pkg/loom-core/src/lockfile.luau
+++ b/lute/cli/commands/pkg/loom-core/src/lockfile.luau
@@ -1,5 +1,7 @@
+local fs = require("@std/fs")
 local luau = require("@std/luau")
 local manifest = require("./manifest")
+local pp = require("@batteries/pp")
 
 export type LockfileDependency = {
 	read name: string,
@@ -10,8 +12,14 @@ export type LockfileDependency = {
 	read dependencies: { [string]: string },
 }
 
+export type LockfileMember = {
+	read path: string,
+	read dependencies: { [string]: string },
+}
+
 export type Lockfile = {
 	read version: number,
+	read members: { [string]: LockfileMember }?,
 	read packages: { [string]: LockfileDependency },
 	read dependencies: { [string]: string },
 }
@@ -56,14 +64,57 @@ local function parseLockfile(t: any): Lockfile
 	end
 	table.freeze(t.dependencies)
 
+	if t.members ~= nil then
+		assert(type(t.members) == "table", "Expected 'members' field to be a table")
+		for memberName, member in t.members do
+			assert(type(memberName) == "string", "Expected member name to be a string")
+			assert(type(member) == "table", `Expected table for member '{memberName}'`)
+			assert(type(member.path) == "string", `Expected 'path' field in member '{memberName}'`)
+			assert(
+				type((member :: { [string]: unknown }).dependencies) == "table",
+				`Expected 'dependencies' field in member '{memberName}'`
+			)
+			for alias, depKey in member.dependencies do
+				assert(type(alias) == "string", `Expected alias to be a string in member '{memberName}'`)
+				assert(type(depKey) == "string", `Expected dependency key to be a string in member '{memberName}'`)
+				assert(
+					(t.packages :: { [string]: unknown })[depKey] ~= nil,
+					`Dependency '{depKey}' in member '{memberName}' not found in packages`
+				)
+			end
+			table.freeze(member.dependencies)
+		end
+		table.freeze(t.members)
+	end
+
 	return table.freeze(t)
 end
 
-local function parseFile(lockfilePath: string): Lockfile
+local function resolveLockfilePath(directory: string): string
+	return `{directory}/loom.lock.luau`
+end
+
+local function parseLockfileFile(directory: string): Lockfile
+	local lockfilePath = resolveLockfilePath(directory)
+	if not fs.exists(lockfilePath) then
+		error(`Lockfile not found at: {lockfilePath}`)
+	end
 	local t = luau.loadModule(lockfilePath)
 	return parseLockfile(t)
 end
 
+local function renderLockfile(lock: Lockfile): string
+	return `return {pp(lock)}\n`
+end
+
+local function writeLockfile(lock: Lockfile, directory: string)
+	local lockfilePath = resolveLockfilePath(directory)
+	fs.writeStringToFile(lockfilePath, renderLockfile(lock))
+end
+
 return {
-	parseFile = parseFile,
+	resolveLockfilePath = resolveLockfilePath,
+	parseLockfile = parseLockfileFile,
+	renderLockfile = renderLockfile,
+	writeLockfile = writeLockfile,
 }
diff --git a/lute/cli/commands/pkg/loom-core/src/manifest.luau b/lute/cli/commands/pkg/loom-core/src/manifest.luau
index 109402c1..05f8625d 100644
--- a/lute/cli/commands/pkg/loom-core/src/manifest.luau
+++ b/lute/cli/commands/pkg/loom-core/src/manifest.luau
@@ -1,3 +1,4 @@
+local fs = require("@std/fs")
 local luau = require("@std/luau")
 
 export type SourceKind = "path" | "github" | "registry"
@@ -32,6 +33,7 @@ export type PackageManifest = {
 
 export type ProjectManifest = {
 	read package: PackageManifest,
+	read workspace: { string }?,
 }
 
 local function isSourceKind(s: unknown): boolean
@@ -103,7 +105,17 @@ local function parseManifest(t: unknown): ProjectManifest
 				})
 			end
 		end
-		table.freeze(dependencies)
+	end
+	table.freeze(dependencies)
+
+	local workspace = (t :: any).workspace
+	if workspace ~= nil then
+		assert(typeof(workspace) == "table", "Expected 'workspace' field to be an array of strings")
+		for i, member in workspace do
+			assert(typeof(i) == "number", "Expected 'workspace' field to be an array of strings")
+			assert(typeof(member) == "string", "Expected each workspace member to be a string path")
+		end
+		table.freeze(workspace)
 	end
 
 	return table.freeze({
@@ -112,16 +124,26 @@ local function parseManifest(t: unknown): ProjectManifest
 			version = pkg.version,
 			dependencies = dependencies,
 		}),
+		workspace = workspace,
 	})
 end
 
-local function parseFile(manifestPath: string): ProjectManifest
-	local manifestTable = luau.loadModule(manifestPath)
-	return parseManifest(manifestTable)
+local function resolveManifestPath(directory: string): string
+	return `{directory}/loom.config.luau`
+end
+
+local function parseManifestFile(directory: string): ProjectManifest
+	local manifestPath = resolveManifestPath(directory)
+	if not fs.exists(manifestPath) then
+		error(`Manifest not found at: {manifestPath}`)
+	end
+	local t = luau.loadModule(manifestPath)
+	return parseManifest(t)
 end
 
 return {
 	isSourceKind = isSourceKind,
 	parseSourceKind = parseSourceKind,
-	parseFile = parseFile,
+	parseManifest = parseManifestFile,
+	resolveManifestPath = resolveManifestPath,
 }
diff --git a/lute/cli/commands/pkg/loom-core/src/resolution.luau b/lute/cli/commands/pkg/loom-core/src/resolution.luau
index 28739386..44deb71a 100644
--- a/lute/cli/commands/pkg/loom-core/src/resolution.luau
+++ b/lute/cli/commands/pkg/loom-core/src/resolution.luau
@@ -1,18 +1,29 @@
 local fs = require("@std/fs")
 local github = require("./github")
 local lockfile = require("./lockfile")
-local path = require("@std/path")
-local pp = require("@batteries/pp")
 local manifest = require("./manifest")
+local path = require("@std/path")
 local semver = require("./semver")
 local store = require("./store")
 
-local function getManifestPath(directory: string): string
-	return `{directory}/loom.config.luau`
-end
+type RegistryFixup = {
+	aliases: { [string]: string },
+	alias: string,
+	name: string,
+}
 
-local function getLockfilePath(directory: string): string
-	return `{directory}/loom.lock.luau`
+type RegistryContext = {
+	requirements: { [string]: semver.Requirement },
+	sources: { [string]: string },
+	fixups: { RegistryFixup },
+}
+
+local function newRegistryContext(): RegistryContext
+	return {
+		requirements = {},
+		sources = {},
+		fixups = {},
+	}
 end
 
 local function getKey(spec: manifest.DependencySpec): string
@@ -41,7 +52,7 @@ end
 
 local function normalizePathDependency(
 	spec: manifest.DependencySpec,
-	installPath: string,
+	contextDir: string,
 	rootDirectory: string
 ): manifest.DependencySpec
 	-- normalization is a no-op if it's not a path dependency
@@ -49,16 +60,93 @@ local function normalizePathDependency(
 		return spec
 	end
 
-	local sourcePath = path.format(path.resolve(installPath, spec.source))
+	local sourcePath = path.format(path.resolve(contextDir, spec.source))
 	if not fs.exists(sourcePath) then
 		error(`Path dependency directory not found: {sourcePath} (for package '{spec.name}')`)
 	end
 
 	return table.freeze({
 		name = spec.name,
-		sourceKind = "path",
+		sourceKind = "path" :: "path",
 		source = path.format(path.relative(rootDirectory, sourcePath)),
-	}) :: manifest.PathDependencySpec
+	})
+end
+
+-- Walk a dependency map and split it: registry specs become solver inputs (with a
+-- pending fixup so we can write the resolved key back into `aliases` post-solve);
+-- path/github specs are normalized and pushed onto the queue.
+local function enqueueDependencies(
+	queue: { manifest.DependencySpec },
+	deps: { [string]: manifest.DependencySpec },
+	contextDir: string,
+	rootDirectory: string,
+	registry: RegistryContext,
+	requiredBy: string,
+	sourceKeyPrefix: string
+): { [string]: string }
+	local aliases: { [string]: string } = {}
+	for alias, depSpec in deps do
+		if depSpec.sourceKind == "registry" then
+			local sourceKey = `{sourceKeyPrefix}:{alias}`
+			registry.requirements[sourceKey] = {
+				name = depSpec.name,
+				constraint = semver.parseConstraint(depSpec.version),
+				requiredBy = requiredBy,
+				aliasedAs = if alias ~= depSpec.name then alias else nil,
+			}
+			registry.sources[depSpec.name] = depSpec.source
+			table.insert(registry.fixups, {
+				aliases = aliases,
+				alias = alias,
+				name = depSpec.name,
+			})
+		else
+			local normalized = normalizePathDependency(depSpec, contextDir, rootDirectory)
+			aliases[alias] = getKey(normalized)
+			table.insert(queue, normalized)
+		end
+	end
+	return aliases
+end
+
+local function seedWorkspaceMembers(
+	queue: { manifest.DependencySpec },
+	nameIndex: { [string]: string },
+	rootDirectory: string,
+	rootManifest: manifest.ProjectManifest,
+	registry: RegistryContext
+): { [string]: lockfile.LockfileMember }?
+	if rootManifest.workspace == nil then
+		return nil
+	end
+	local members: { [string]: lockfile.LockfileMember } = {}
+	for _, memberPath in rootManifest.workspace do
+		local memberDir = path.format(path.resolve(rootDirectory, memberPath))
+		local memberManifest = manifest.parseManifest(memberDir)
+		local memberName = memberManifest.package.name
+		if members[memberName] then
+			error(
+				`Duplicate workspace member name '{memberName}':\n`
+					.. `  {members[memberName].path}\n`
+					.. `  {memberPath}`
+			)
+		end
+		local memberDeps = enqueueDependencies(
+			queue,
+			memberManifest.package.dependencies,
+			memberDir,
+			rootDirectory,
+			registry,
+			memberName,
+			`member:{memberName}`
+		)
+		members[memberName] = table.freeze({
+			path = memberPath,
+			dependencies = memberDeps,
+		})
+		nameIndex[memberName] = memberName
+	end
+	return table.freeze(members)
 end
 
 local function fetchSource(spec: manifest.DependencySpec, rootDirectory: string): string
@@ -83,99 +171,59 @@ local function fetchSource(spec: manifest.DependencySpec, rootDirectory: string)
 	end
 end
 
-local function resolve(rootDirectory: string, universe: semver.Universe?): lockfile.Lockfile
-	local rootManifestPath = getManifestPath(rootDirectory)
-	if not fs.exists(rootManifestPath) then
-		error(`Manifest file not found: {rootManifestPath}`)
+local function shouldDeduplicate(
+	key: string,
+	spec: manifest.DependencySpec,
+	packages: { [string]: lockfile.LockfileDependency },
+	nameIndex: { [string]: string }
+): boolean
+	if packages[key] then
+		if hasConflict(packages[key], spec) then
+			error(
+				`Source mismatch for package '{spec.name}':\n`
+					.. `  resolved: {packages[key].source} ({packages[key].sourceKind})\n`
+					.. `  incoming: {spec.source} ({spec.sourceKind})\n`
+					.. `Both resolve to '{key}' but from different sources.`
+			)
+		else
+			-- Already resolved at this exact version — deduplicate
+			return true
+		end
 	end
 
-	local lockfilePath = getLockfilePath(rootDirectory)
-
-	local queue: { manifest.DependencySpec } = {}
-	local dependencies: { [string]: string } = {}
-	local packages: { [string]: lockfile.LockfileDependency } = {}
-
-	-- All registry solver inputs: root-level dependencies keyed by alias, transitive by "{pkgKey}:{dependencyAlias}".
-	local registryRequirements: { [string]: semver.Requirement } = {}
-	local registrySources: { [string]: string } = {}
-
-	-- Root aliases that need a resolved key written back into `dependencies` after solving.
-	local rootRegistryAliases: { string } = {}
-	-- Per-package registry aliases that need keys written into their dependency table after solving.
-	local unresolvedRegistryDependencies: { [string]: { [string]: string } } = {}
-	-- Open (not yet finalized) dependency maps for unversioned packages; registry entries are filled in during versioned resolution.
-	local openDependencies: { [string]: { [string]: string } } = {}
-
-	local rootManifest = manifest.parseFile(rootManifestPath)
-
-	for dependencyAlias, dependencySpec in rootManifest.package.dependencies do
-		if dependencySpec.sourceKind == "registry" then
-			registryRequirements[dependencyAlias] = {
-				name = dependencySpec.name,
-				constraint = semver.parseConstraint(dependencySpec.version),
-				requiredBy = rootManifest.package.name,
-				aliasedAs = if dependencyAlias ~= dependencySpec.name then dependencyAlias else nil,
-			}
+	local conflictKey = nameIndex[spec.name]
+	if conflictKey then
+		error(
+			`Version conflict for package '{spec.name}':\n`
+				.. `  resolved: {conflictKey}\n`
+				.. `  incoming: {key}\n`
+				.. `Resolve by pinning both to the same revision.`
+		)
+	end
 
-			registrySources[dependencySpec.name] = dependencySpec.source
-			table.insert(rootRegistryAliases, dependencyAlias)
-			continue
-		end
+	-- new dependency
+	nameIndex[spec.name] = key
+	return false
+end
 
-		dependencies[dependencyAlias] = getKey(dependencySpec)
-		table.insert(queue, dependencySpec)
-	end
+local function processSpecQueue(
+	queue: { manifest.DependencySpec },
+	nameIndex: { [string]: string },
+	rootDirectory: string,
+	registry: RegistryContext
+): { [string]: lockfile.LockfileDependency }
+	local packages: { [string]: lockfile.LockfileDependency } = {}
 
 	for _, spec in queue do
 		local key = getKey(spec)
-
-		if packages[key] then
-			if hasConflict(packages[key], spec) then
-				error(
-					`Source mismatch for package '{spec.name}':\n`
-						.. `  resolved: {packages[key].source} ({packages[key].sourceKind})\n`
-						.. `  incoming: {spec.source} ({spec.sourceKind})\n`
-						.. `Both resolve to '{key}' but from different sources.`
-				)
-			else
-				-- Already resolved at this exact version — deduplicate
-				continue
-			end
+		if shouldDeduplicate(key, spec, packages, nameIndex) then
+			continue
 		end
 
-		local transitiveDependencies: { [string]: string } = {}
-		openDependencies[key] = transitiveDependencies
-
 		local installPath = fetchSource(spec, rootDirectory)
-
-		local nestedManifestPath = getManifestPath(installPath)
-		if fs.exists(nestedManifestPath) then
-			local dependencyManifest = manifest.parseFile(nestedManifestPath)
-			for dependencyAlias, dependencySpec in dependencyManifest.package.dependencies do
-				local normalizedSpec = normalizePathDependency(dependencySpec, installPath, rootDirectory)
-
-				if normalizedSpec.sourceKind == "registry" then
-					registryRequirements[`{key}:{dependencyAlias}`] = {
-						name = normalizedSpec.name,
-						constraint = semver.parseConstraint(normalizedSpec.version),
-						requiredBy = key,
-						aliasedAs = if dependencyAlias ~= normalizedSpec.name then dependencyAlias else nil,
-					}
-
-					registrySources[normalizedSpec.name] = normalizedSpec.source
-
-					if not unresolvedRegistryDependencies[key] then
-						unresolvedRegistryDependencies[key] = {}
-					end
-
-					unresolvedRegistryDependencies[key][dependencyAlias] = normalizedSpec.name
-					continue
-				end
-
-				transitiveDependencies[dependencyAlias] = getKey(normalizedSpec)
-				table.insert(queue, normalizedSpec)
-			end
-		end
+		local depManifest = manifest.parseManifest(installPath)
+		local transitiveDependencies =
+			enqueueDependencies(queue, depManifest.package.dependencies, installPath, rootDirectory, registry, key, key)
 
 		packages[key] = table.freeze({
 			name = spec.name,
@@ -189,68 +237,93 @@ local function resolve(rootDirectory: string, universe: semver.Universe?): lockf
 		})
 	end
 
-	-- Versioned dependency resolution: solve registry dependencies and stitch them into the package graph.
-	if next(registryRequirements) ~= nil then
-		assert(universe ~= nil, "Registry dependencies require a universe to be provided")
-
-		local resolution = semver.solve(universe, registryRequirements)
+	-- "destroy" queue
+	table.clear(queue)
+	table.freeze(queue)
 
-		for name, pkgVersion in resolution do
-			local key = `{name}@{pkgVersion.versionString}`
+	return packages
+end
 
-			local transitiveDependencies: { [string]: string } = {}
-			for alias, dependency in pkgVersion.dependencies do
-				local resolvedVersion = resolution[dependency.name]
-				assert(resolvedVersion ~= nil, `Solver produced an incomplete resolution: missing '{dependency.name}'`)
+-- Solve all collected registry requirements in one pass and stitch the resolved
+-- versions into the package graph: emit one packages entry per resolved version,
+-- and back-patch every fixup's alias table with the concrete `name@version` key.
+local function solveRegistryRequirements(
+	packages: { [string]: lockfile.LockfileDependency },
+	registry: RegistryContext,
+	universe: semver.Universe
+)
+	local resolution = semver.solve(universe, registry.requirements)
 
-				transitiveDependencies[alias] = `{dependency.name}@{resolvedVersion.versionString}`
-			end
+	for name, pkgVersion in resolution do
+		local key = `{name}@{pkgVersion.versionString}`
 
-			packages[key] = table.freeze({
-				name = name,
-				rev = pkgVersion.versionString,
-				source = registrySources[name] or name,
-				sourceKind = "registry" :: "registry",
-				installPath = store.toStorePath(key),
-				dependencies = table.freeze(transitiveDependencies),
-			})
+		local transitiveDependencies: { [string]: string } = {}
+		for alias, dependency in pkgVersion.dependencies do
+			local resolvedDep = resolution[dependency.name]
+			assert(resolvedDep ~= nil, `Solver produced an incomplete resolution: missing '{dependency.name}'`)
+			transitiveDependencies[alias] = `{dependency.name}@{resolvedDep.versionString}`
 		end
 
-		-- Fill in pending registry dependency keys directly into the unversioned packages' dependency tables.
-		for pkgKey, unresolvedAliases in unresolvedRegistryDependencies do
-			local packageDependencies = openDependencies[pkgKey]
-			for dependencyAlias, registryName in unresolvedAliases do
-				local pkgVersion = resolution[registryName]
-				assert(pkgVersion ~= nil, `Solver did not resolve transitive registry dependency '{registryName}'`)
-				packageDependencies[dependencyAlias] = `{registryName}@{pkgVersion.versionString}`
-			end
-		end
+		packages[key] = table.freeze({
+			name = name,
+			rev = pkgVersion.versionString,
+			source = registry.sources[name] or name,
+			sourceKind = "registry" :: "registry",
+			installPath = store.toStorePath(key),
+			dependencies = transitiveDependencies,
+		})
+	end
 
-		-- Write root-level registry dependency aliases now that versions are resolved.
-		for _, alias in rootRegistryAliases do
-			local req = registryRequirements[alias]
-			local pkgVersion = resolution[req.name]
-			assert(pkgVersion ~= nil, `Solver did not resolve root dependency '{req.name}'`)
-			dependencies[alias] = `{req.name}@{pkgVersion.versionString}`
-		end
+	for _, fix in registry.fixups do
+		local pkgVersion = resolution[fix.name]
+		assert(pkgVersion ~= nil, `Solver did not resolve registry dependency '{fix.name}'`)
+		fix.aliases[fix.alias] = `{fix.name}@{pkgVersion.versionString}`
 	end
+end
 
-	for _, packageDependencies in openDependencies do
-		table.freeze(packageDependencies)
+local function resolve(rootDirectory: string, universe: semver.Universe?): lockfile.Lockfile
+	local queue: { manifest.DependencySpec } = {}
+	local nameIndex: { [string]: string } = {}
+	local registry = newRegistryContext()
+
+	local rootManifest = manifest.parseManifest(rootDirectory)
+
+	local dependencies = enqueueDependencies(
+		queue,
+		rootManifest.package.dependencies,
+		rootDirectory,
+		rootDirectory,
+		registry,
+		rootManifest.package.name,
+		"root"
+	)
+	local members = seedWorkspaceMembers(queue, nameIndex, rootDirectory, rootManifest, registry)
+	local packages = processSpecQueue(queue, nameIndex, rootDirectory, registry)
+
+	if next(registry.requirements) ~= nil then
+		assert(universe ~= nil, "Registry dependencies require a universe to be provided")
+		solveRegistryRequirements(packages, registry, universe)
 	end
 
-	table.freeze(packages)
+	-- Freeze alias tables that we deliberately left open so registry fixups could
+	-- write back into them. Package entries are already shallow-frozen.
 	table.freeze(dependencies)
+	if members ~= nil then
+		for _, member in members do
+			table.freeze(member.dependencies)
+		end
+	end
+	for _, pkg in packages do
+		table.freeze(pkg.dependencies)
+	end
+	table.freeze(packages)
 
-	local lock: lockfile.Lockfile = table.freeze({
+	return table.freeze({
 		version = 3,
+		members = members,
 		packages = packages,
 		dependencies = dependencies,
 	})
-
-	fs.writeStringToFile(lockfilePath, `return {pp(lock)}\n`)
-
-	return lock
 end
 
 return {
diff --git a/lute/cli/include/lute/packagerun.h b/lute/cli/include/lute/packagerun.h
index ee6052b8..67e3f27a 100644
--- a/lute/cli/include/lute/packagerun.h
+++ b/lute/cli/include/lute/packagerun.h
@@ -10,5 +10,6 @@
 std::optional<std::string> getAbsolutePathToNearestLockfile(std::string entryFile);
 
 std::pair<std::vector<Package::Identifier>, std::vector<std::pair<Package::Identifier, Package::Info>>> getDependenciesFromLockfile(
-    const std::string& lockfilePath
+    const std::string& lockfilePath,
+    const std::string& entryFilePath
 );
diff --git a/lute/cli/src/climain.cpp b/lute/cli/src/climain.cpp
index 376f8eb7..524722c3 100644
--- a/lute/cli/src/climain.cpp
+++ b/lute/cli/src/climain.cpp
@@ -437,7 +437,7 @@ int handleRunCommand(int argc, char** argv, int argOffset, bool packageAwareness
             return 1;
         }
 
-        auto [directDependencies, allDependencies] = getDependenciesFromLockfile(*lockfile);
+        auto [directDependencies, allDependencies] = getDependenciesFromLockfile(*lockfile, validPath);
         setupPkgRunState(runtime, std::move(directDependencies), std::move(allDependencies));
     }
     else
diff --git a/lute/cli/src/packagerun.cpp b/lute/cli/src/packagerun.cpp
index 4dff4135..055931a0 100644
--- a/lute/cli/src/packagerun.cpp
+++ b/lute/cli/src/packagerun.cpp
@@ -96,7 +96,8 @@ static std::string getEntryPoint(const std::string& packageRoot)
 }
 
 std::pair<std::vector<Package::Identifier>, std::vector<std::pair<Package::Identifier, Package::Info>>> getDependenciesFromLockfile(
-    const std::string& lockfilePath
+    const std::string& lockfilePath,
+    const std::string& entryFilePath
 )
 {
     LUTE_ASSERT(isFile(lockfilePath));
@@ -226,6 +227,62 @@ std::pair<std::vector<Package::Identifier>, std::vector<std::pair<Package::Ident
             directDependencies.push_back(it->second);
     }
 
+    // If the entry file is inside a workspace member, override direct dependencies
+    // with that member's dependencies instead of the root's.
+    if (!entryFilePath.empty() && config.contains("members"))
+    {
+        Luau::ConfigTable* membersTable = config["members"].get_if<Luau::ConfigTable>();
+        if (membersTable)
+        {
+            std::string normalizedEntry = normalizePath(entryFilePath);
+            std::string bestMemberPath;
+            const Luau::ConfigTable* bestMember = nullptr;
+
+            for (const auto& [mk, mv] : *membersTable)
+            {
+                const Luau::ConfigTable* member = mv.get_if<Luau::ConfigTable>();
+                if (!member || !member->contains("path") || !member->contains("dependencies"))
+                    continue;
+                const std::string* memberPath = (*member).find("path")->get_if<std::string>();
+                if (!memberPath)
+                    continue;
+                std::string resolvedMemberPath = normalizePath(joinPaths(*lockfileParentDir, *memberPath));
+                if (!resolvedMemberPath.empty() && resolvedMemberPath.back() != '/')
+                    resolvedMemberPath += '/';
+                if (normalizedEntry.substr(0, resolvedMemberPath.size()) == resolvedMemberPath &&
+                    resolvedMemberPath.size() > bestMemberPath.size())
+                {
+                    bestMemberPath = resolvedMemberPath;
+                    bestMember = member;
+                }
+            }
+
+            if (bestMember)
+            {
+                const Luau::ConfigTable* memberDeps = (*bestMember).find("dependencies")->get_if<Luau::ConfigTable>();
+                if (memberDeps)
+                {
+                    directDependencies.clear();
+                    for (const auto& [dk, dv] : *memberDeps)
+                    {
+                        const std::string* alias = dk.get_if<std::string>();
+                        const std::string* depKey = dv.get_if<std::string>();
+                        if (!alias || !depKey)
+                            continue;
+                        auto it = keyToIdentifier.find(*depKey);
+                        if (it != keyToIdentifier.end())
+                        {
+                            Package::Identifier aliasedId;
+                            aliasedId.name = toLower(*alias);
+                            aliasedId.version = it->second.version;
+                            directDependencies.push_back(std::move(aliasedId));
+                        }
+                    }
+                }
+            }
+        }
+    }
+
     // Build all dependencies. Each package is registered under its canonical
     // name. Additionally, register alias entries so that require("@alias")
     // can find the package by the alias name used in the lockfile.
diff --git a/tests/cli/pkg/install.test.luau b/tests/cli/pkg/install.test.luau
index 8b560310..5c329ee5 100644
--- a/tests/cli/pkg/install.test.luau
+++ b/tests/cli/pkg/install.test.luau
@@ -31,7 +31,7 @@ test.suite("LutePkgInstall", function(suite)
 		local result = run(workingDirectory)
 
 		assert.eq(result.exitcode, 1)
-		assert.strContains(result.stderr, "Manifest file not found")
+		assert.strContains(result.stderr, "Manifest not found")
 	end)
 
 	suite:case("succeedsWithNoDependencies", function(assert)
diff --git a/tests/cli/pkg/resolution.test.luau b/tests/cli/pkg/resolution.test.luau
index 6e58dff8..046b7c6c 100644
--- a/tests/cli/pkg/resolution.test.luau
+++ b/tests/cli/pkg/resolution.test.luau
@@ -3,17 +3,19 @@ local path = require("@std/path")
 local system = require("@std/system")
 local test = require("@std/test")
 
+local lockfile = require("@commands/pkg/loom-core/src/lockfile")
 local resolution = require("@commands/pkg/loom-core/src/resolution")
 local parser = require("@commands/pkg/loom-core/src/semver/parser")
 local solver = require("@commands/pkg/loom-core/src/semver/solver")
 
 local temporaryDirectory = system.tmpdir()
-local workingDirectory = path.join(temporaryDirectory, "lute-pkg-registry")
+local workingDirectory = path.join(temporaryDirectory, "lute-pkg-resolution")
 
-local function writeConfig(dir: path.Path, name: string, deps: string)
+local function writeConfig(dir: path.Path, name: string, deps: string, workspace: string?)
+	local workspaceField = if workspace ~= nil then `,\n\tworkspace = \{{workspace}\n\t}` else ""
 	fs.writeStringToFile(
 		path.join(dir, "loom.config.luau"),
-		`return \{\n\tpackage = \{\n\t\tname = "{name}",\n\t\tversion = "0.0.0",\n\t\tdependencies = \{{deps}\n\t\t},\n\t},\n}\n`
+		`return \{\n\tpackage = \{\n\t\tname = "{name}",\n\t\tversion = "0.0.0",\n\t\tdependencies = \{{deps}\n\t\t},\n\t}{workspaceField},\n}\n`
 	)
 end
 
@@ -34,14 +36,133 @@ local function req(name: string, constraintStr: string): solver.Requirement
 	})
 end
 
-test.suite("RegistryResolution", function(suite)
-	suite:beforeEach(function()
-		if fs.exists(workingDirectory) then
-			fs.removeDirectory(workingDirectory, { recursive = true })
+local function resetWorkingDirectory()
+	if fs.exists(workingDirectory) then
+		fs.removeDirectory(workingDirectory, { recursive = true })
+	end
+	fs.createDirectory(workingDirectory)
+end
+
+test.suite("LoomResolution", function(suite)
+	suite:beforeEach(resetWorkingDirectory)
+
+	suite:case("workspaceMembersAppearInLockfile", function(assert)
+		local pkgsDir = path.join(workingDirectory, "packages")
+		fs.createDirectory(pkgsDir)
+		fs.createDirectory(path.join(pkgsDir, "a"))
+		fs.createDirectory(path.join(pkgsDir, "b"))
+		writeConfig(path.join(pkgsDir, "a"), "a", "")
+		writeConfig(path.join(pkgsDir, "b"), "b", "")
+		writeConfig(
+			workingDirectory,
+			"root",
+			"",
+			[[
+
+		"./packages/a",
+		"./packages/b",]]
+		)
+
+		local lock = resolution.resolve(path.format(workingDirectory))
+
+		assert.that(lock.members ~= nil)
+		local members = lock.members :: any
+		assert.eq(members.a.path, "./packages/a")
+		assert.eq(members.b.path, "./packages/b")
+	end)
+
+	suite:case("workspaceFailsOnDuplicateMemberName", function(assert)
+		local pkgsDir = path.join(workingDirectory, "packages")
+		fs.createDirectory(pkgsDir)
+		fs.createDirectory(path.join(pkgsDir, "one"))
+		fs.createDirectory(path.join(pkgsDir, "two"))
+		-- Both members declare package.name = "shared"
+		writeConfig(path.join(pkgsDir, "one"), "shared", "")
+		writeConfig(path.join(pkgsDir, "two"), "shared", "")
+		writeConfig(
+			workingDirectory,
+			"root",
+			"",
+			[[
+
+		"./packages/one",
+		"./packages/two",]]
+		)
+
+		local ok, err = pcall(resolution.resolve, path.format(workingDirectory), nil)
+		assert.eq(ok, false)
+		assert.strContains(tostring(err), "Duplicate workspace member name 'shared'")
+	end)
+
+	suite:case("transitiveWorkspaceMembersDoNotLeak", function(assert)
+		-- libW is itself a workspace, but we depend on libW only as a path dep.
+		-- libW's internal member `libCore` should NOT appear in the root lockfile.
+		local libDir = path.join(workingDirectory, "libW")
+		fs.createDirectory(libDir)
+		fs.createDirectory(path.join(libDir, "core"))
+		writeConfig(path.join(libDir, "core"), "libCore", "")
+		writeConfig(
+			libDir,
+			"libW",
+			"",
+			[[
+
+		"./core",]]
+		)
+
+		writeConfig(
+			workingDirectory,
+			"root",
+			[[
+
+			libW = {
+				sourceKind = "path",
+				source = "./libW",
+			},]]
+		)
+
+		local lock = resolution.resolve(path.format(workingDirectory))
+
+		assert.that(lock.packages.libW ~= nil)
+		assert.eq(lock.members, nil)
+		-- libCore is libW's internal member; it should not leak into root's graph
+		for _, pkg in lock.packages do
+			assert.never.eq(pkg.name, "libCore")
 		end
-		fs.createDirectory(workingDirectory)
 	end)
 
+	suite:case("memberNameCollidesWithPathDep", function(assert)
+		local memberDir = path.join(workingDirectory, "internal")
+		local extDir = path.join(workingDirectory, "ext")
+		fs.createDirectory(memberDir)
+		fs.createDirectory(extDir)
+		-- Both packages declare package.name = "shared"
+		writeConfig(memberDir, "shared", "")
+		writeConfig(extDir, "shared", "")
+
+		writeConfig(
+			workingDirectory,
+			"root",
+			[[
+
+			shared = {
+				sourceKind = "path",
+				source = "./ext",
+			},]],
+			[[
+
+		"./internal",]]
+		)
+
+		local ok, err = pcall(resolution.resolve, path.format(workingDirectory), nil)
+		assert.eq(ok, false)
+		assert.strContains(tostring(err), "Version conflict")
+	end)
+end)
+
+test.suite("RegistryResolution", function(suite)
+	suite:beforeEach(resetWorkingDirectory)
+
 	suite:case("resolvesHighestSatisfyingVersion", function(assert)
 		writeConfig(
 			workingDirectory,
@@ -438,7 +559,7 @@ test.suite("RegistryResolution", function(suite)
 		assert.eq(lock.dependencies.b, "citrus@1.0.0")
 	end)
 
-	suite:case("writesLockfileToDisk", function(assert)
+	suite:case("writeLockfilePersistsRegistryEntries", function(assert)
 		writeConfig(
 			workingDirectory,
 			"root",
@@ -457,7 +578,8 @@ test.suite("RegistryResolution", function(suite)
 			},
 		}
 
-		resolution.resolve(path.format(workingDirectory), universe)
+		local lock = resolution.resolve(path.format(workingDirectory), universe)
+		lockfile.writeLockfile(lock, path.format(workingDirectory))
 
 		local lockfilePath = path.join(workingDirectory, "loom.lock.luau")
 		assert.that(fs.exists(lockfilePath))
diff --git a/tests/src/packagerequire.test.cpp b/tests/src/packagerequire.test.cpp
index 65dff337..f494d302 100644
--- a/tests/src/packagerequire.test.cpp
+++ b/tests/src/packagerequire.test.cpp
@@ -123,3 +123,28 @@ TEST_CASE_FIXTURE(LuteFixture, "pkgrun_deep_path_deps")
 
     CHECK_EQ(cliMain(argv.size(), argv.data(), getReporter()), 0);
 }
+
+TEST_CASE_FIXTURE(LuteFixture, "pkgrun_workspace")
+{
+    std::string entry = getLuteProjectRootAbsolute() + "/tests/src/packages/pkgrun_workspace/packages/app/entry.luau";
+
+    char executablePlaceholder[] = "lute";
+    char command[] = "pkg";
+    char subcommand[] = "run";
+    std::vector<char*> argv = {executablePlaceholder, command, subcommand, entry.data()};
+
+    CHECK_EQ(cliMain(argv.size(), argv.data(), getReporter()), 0);
+}
+
+TEST_CASE_FIXTURE(LuteFixture, "pkgrun_workspace_sibling_prefix")
+{
+    std::string entry =
+        getLuteProjectRootAbsolute() + "/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/foobar/entry.luau";
+
+    char executablePlaceholder[] = "lute";
+    char command[] = "pkg";
+    char subcommand[] = "run";
+    std::vector<char*> argv = {executablePlaceholder, command, subcommand, entry.data()};
+
+    CHECK_EQ(cliMain(argv.size(), argv.data(), getReporter()), 0);
+}
diff --git a/tests/src/packages/pkgrun_workspace/loom.lock.luau b/tests/src/packages/pkgrun_workspace/loom.lock.luau
new file mode 100644
index 00000000..e28f0a84
--- /dev/null
+++ b/tests/src/packages/pkgrun_workspace/loom.lock.luau
@@ -0,0 +1,25 @@
+return {
+	version = 3,
+	members = {
+		["lib"] = {
+			path = "packages/lib",
+			dependencies = {},
+		},
+		["app"] = {
+			path = "packages/app",
+			dependencies = {
+				lib = "lib",
+			},
+		},
+	},
+	packages = {
+		["lib"] = {
+			name = "lib",
+			source = "packages/lib",
+			sourceKind = "path",
+			installPath = "packages/lib",
+			dependencies = {},
+		},
+	},
+	dependencies = {},
+}
diff --git a/tests/src/packages/pkgrun_workspace/packages/app/entry.luau b/tests/src/packages/pkgrun_workspace/packages/app/entry.luau
new file mode 100644
index 00000000..76f6c0ad
--- /dev/null
+++ b/tests/src/packages/pkgrun_workspace/packages/app/entry.luau
@@ -0,0 +1,3 @@
+--!strict
+local result = require("@lib") :: string
+assert(result == "hello from lib")
diff --git a/tests/src/packages/pkgrun_workspace/packages/lib/src/init.luau b/tests/src/packages/pkgrun_workspace/packages/lib/src/init.luau
new file mode 100644
index 00000000..bbdaa915
--- /dev/null
+++ b/tests/src/packages/pkgrun_workspace/packages/lib/src/init.luau
@@ -0,0 +1 @@
+return "hello from lib"
diff --git a/tests/src/packages/pkgrun_workspace_sibling_prefix/loom.lock.luau b/tests/src/packages/pkgrun_workspace_sibling_prefix/loom.lock.luau
new file mode 100644
index 00000000..ba635257
--- /dev/null
+++ b/tests/src/packages/pkgrun_workspace_sibling_prefix/loom.lock.luau
@@ -0,0 +1,21 @@
+return {
+	version = 3,
+	members = {
+		["foo"] = {
+			path = "packages/foo",
+			dependencies = {},
+		},
+	},
+	packages = {
+		["lib"] = {
+			name = "lib",
+			source = "packages/lib",
+			sourceKind = "path",
+			installPath = "packages/lib",
+			dependencies = {},
+		},
+	},
+	dependencies = {
+		lib = "lib",
+	},
+}
diff --git a/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/foo/init.luau b/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/foo/init.luau
new file mode 100644
index 00000000..8ec1c159
--- /dev/null
+++ b/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/foo/init.luau
@@ -0,0 +1 @@
+return "Hello from foo"
diff --git a/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/foobar/entry.luau b/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/foobar/entry.luau
new file mode 100644
index 00000000..76f6c0ad
--- /dev/null
+++ b/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/foobar/entry.luau
@@ -0,0 +1,3 @@
+--!strict
+local result = require("@lib") :: string
+assert(result == "hello from lib")
diff --git a/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/lib/src/init.luau b/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/lib/src/init.luau
new file mode 100644
index 00000000..bbdaa915
--- /dev/null
+++ b/tests/src/packages/pkgrun_workspace_sibling_prefix/packages/lib/src/init.luau
@@ -0,0 +1 @@
+return "hello from lib"
