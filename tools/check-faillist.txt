Failed to resolve require: @dep is not a valid alias
Failed to resolve require: @internaldep is not a valid alias
Failed to resolve require: could not resolve alias "dep" (ambiguous configuration file)
Failed to resolve require: could not resolve child component ".config"
Failed to resolve require: could not jump to alias "~/lute_test_special"
Failed to resolve require: could not resolve child component "dependency" (ambiguous)
Failed to resolve require: could not resolve child component "dependency" (ambiguous)
/Users/vvijay/git/roblox/lute/lute/std/libs/path/posix/init.luau:8:22-38: (W0) TypeError: Unknown type 'posixtypes.pathMT'

/Users/vvijay/git/roblox/lute/lute/std/libs/path/win32/init.luau:9:22-38: (W0) TypeError: Unknown type 'win32types.pathMT'

./lute/std/libs/fs.luau:82:3-14: (W0) TypeError: No valid instantiation could be inferred for generic type parameter V. It was expected to be at least:
	{ event: WatchEvent, filename: path | path }
and at most:
	never
but these types are not compatible with one another.

./lute/std/libs/fs.luau:90:17-28: (W0) TypeError: No valid instantiation could be inferred for generic type parameter V. It was expected to be at least:
	
and at most:
	never
but these types are not compatible with one another.

./lute/std/libs/fs.luau:91:11-20: (W0) TypeError: Value of type 'unknown?' could be nil

./lute/std/libs/fs.luau:91:11-20: (W0) TypeError: Type 'unknown' does not have key 'event'

./lute/std/libs/fs.luau:135:34-39: (W0) TypeError: Expected this to be
	'path | pathdata | string'
but got
	'path | path'; 
this is because 
	 * in the 1st component of the union, the table portion is `pathdata` and in the 3rd component of the union, the table portion is `pathdata`, and `pathdata` is not a subtype of `pathdata`
	 * the 1st component of the union is `path` and the 2nd component of the union is `pathdata`, and `path` is not a subtype of `pathdata`
	 * the 1st component of the union is `path` in the latter type and `string` in the former type, and `path` is not a subtype of `string`

./lute/std/libs/fs.luau:184:52-68: (W0) TypeError: Value of type 'walkoptions?' could be nil

./lute/std/libs/fs.luau:192:11-17: (W0) TypeError: Expected this to be
	'(path | path)?'
but got
	'path | path | pathdata | pathdata | string'; 
this is because 
	 * the 1st component of the union is `string` and the 2nd component of the union is `path`, and `string` is not a subtype of `path`
	 * the 1st component of the union is `string` and the 3rd component of the union is `nil`, and `string` is not a subtype of `nil`
	 * the 1st component of the union is `string` in the latter type and `path` in the former type, and `string` is not a subtype of `path`
	 * the 3rd component of the union is `pathdata` and the 1st component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 3rd component of the union is `pathdata` and the 2nd component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 3rd component of the union is `pathdata` in the latter type and `nil` in the former type, and `pathdata` is not a subtype of `nil`
	 * the 4th component of the union is `pathdata` and the 1st component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 4th component of the union is `pathdata` and the 2nd component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 4th component of the union is `pathdata` and the 3rd component of the union is `nil`, and `pathdata` is not a subtype of `nil`

./lute/std/libs/path/posix/init.luau:8:22-38: (W0) TypeError: Unknown type 'posixtypes.pathMT'

./lute/std/libs/path/win32/init.luau:9:22-38: (W0) TypeError: Unknown type 'win32types.pathMT'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:239:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"break" | nil
and at most:
	"break"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:245:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"continue" | nil
and at most:
	"continue"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:376:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"nil" | nil
and at most:
	"nil"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:382:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"false" | "true" | nil
and at most:
	"false" | "true"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:388:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	string | nil
and at most:
	string
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:406:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"..." | nil
and at most:
	"..."
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:475:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"@checked" | "@deprecated" | "@native" | nil
and at most:
	"@checked" | "@deprecated" | "@native"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:650:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"false" | "true" | nil
and at most:
	"false" | "true"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:656:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	string | nil
and at most:
	string
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:679:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"?" | nil
and at most:
	"?"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:938:24-28: (W0) TypeError: Expected this to be
	'(AstStatBlock) -> ()'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
it returns `boolean` in the latter type and `()` in the former type, and `boolean` is not a subtype of `()`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:947:35-39: (W0) TypeError: Expected this to be
	'(AstStatLocal) -> ()'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
it returns `boolean` in the latter type and `()` in the former type, and `boolean` is not a subtype of `()`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:959:19-23: (W0) TypeError: Expected this to be
	'({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
it returns `boolean` in the latter type and `()` in the former type, and `boolean` is not a subtype of `()`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:966:25-29: (W0) TypeError: Expected this to be
	'(AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 12th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 13th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 14th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 15th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 16th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 17th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 18th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `AstExprGroup` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"nil">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"false" | "true">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 1st component of the intersection as `{ kind: "expr" }`, and `boolean` is not exactly `{ kind: "expr" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `boolean` is not exactly `AstExprIndexName`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `boolean` is not exactly `AstExprIndexExpr`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `boolean` is not exactly `AstExprAnonymousFunction`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable`, and `boolean` is not exactly `AstExprTable`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `boolean` is not exactly `AstExprUnary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `boolean` is not exactly `AstExprBinary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `boolean` is not exactly `AstExprInterpString`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `boolean` is not exactly `AstExprTypeAssertion`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `boolean` is not exactly `AstExprIfElse`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `boolean` is not exactly `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `boolean` is not exactly `Token<"nil">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `boolean` is not exactly `Token<"false" | "true">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `boolean` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `boolean` is not exactly `AstExprLocal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `boolean` is not exactly `AstExprGlobal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `boolean` is not exactly `Token<"...">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall`, and `boolean` is not exactly `AstExprCall`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<string>` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 1st component of the intersection as `{ kind: "expr" }`, and `number` is not exactly `{ kind: "expr" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `number` is not exactly `AstExprIndexName`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `number` is not exactly `AstExprIndexExpr`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `number` is not exactly `AstExprAnonymousFunction`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable`, and `number` is not exactly `AstExprTable`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `number` is not exactly `AstExprUnary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `number` is not exactly `AstExprBinary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `number` is not exactly `AstExprInterpString`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `number` is not exactly `AstExprTypeAssertion`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `number` is not exactly `AstExprIfElse`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `number` is not exactly `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `number` is not exactly `Token<"nil">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `number` is not exactly `Token<"false" | "true">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `number` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `number` is not exactly `AstExprLocal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `number` is not exactly `AstExprGlobal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `number` is not exactly `Token<"...">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall`, and `number` is not exactly `AstExprCall`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<string>` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 6th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 7th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"...">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectionkind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersection as `Token<"break">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"break">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersectionkind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersection as `Token<"continue">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"continue">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersectionkind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectionkind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `kind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"false" | "true">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"false" | "true">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionkind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionkind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `kind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union, accessing `kind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:970:21-25: (W0) TypeError: Expected this to be
	'(AstExprGroup) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersection as `{ kind: "expr" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "expr" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `{ kind: "stat" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "stat" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `tag` results in `"local"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"local"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `tag` results in `"for"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"for"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union, accessing `tag` results in `"forin"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"forin"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union, accessing `tag` results in `"assign"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"assign"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union, accessing `tag` results in `"compoundassign"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"compoundassign"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union, accessing `tag` results in `"function"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"function"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union, accessing `tag` results in `"localfunction"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"localfunction"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union, accessing `tag` results in `"typealias"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"typealias"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union, accessing `tag` results in `"typefunction"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"typefunction"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `tag` results in `"block"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"block"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union, accessing `tag` results in `"do"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"do"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union, accessing `tag` results in `"conditional"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"conditional"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `tag` results in `"while"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"while"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union, accessing `tag` results in `"repeat"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"repeat"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersection as `Token<"break">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"break">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersectiontag` results in `"break"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"break"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersection as `Token<"continue">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"continue">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersectiontag` results in `"continue"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"continue"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union, accessing `tag` results in `"return"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"return"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `tag` results in `"expression"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"expression"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `{ kind: "type" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "type" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `tag` results in `"table"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"table"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `tag` results in `"function"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"function"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `tag` results in `"reference"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"reference"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"false" | "true">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"false" | "true">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectiontag` results in `"boolean"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"boolean"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<string>` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<string>` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectiontag` results in `"string"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"string"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `tag` results in `"typeof"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"typeof"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstTypeGroup` and it takes the 1st entry in the type pack is `AstExprGroup`, and `AstTypeGroup` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 6th component of the union, accessing `tag` results in `"union"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"union"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union, accessing `tag` results in `"intersection"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"intersection"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"?">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"?">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersectiontag` results in `"optional"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"optional"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `tag` results in `"array"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"array"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `{ kind: "typepack" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "typepack" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `closeparens` has the 2nd component of the union as `nil` and it takes the 1st entry in the type pack, accessing `closeparens` results in `Token<")">`, and `nil` is not exactly `Token<")">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `openparens` has the 2nd component of the union as `nil` and it takes the 1st entry in the type pack, accessing `openparens` results in `Token<"(">`, and `nil` is not exactly `Token<"(">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `tag` results in `"explicit"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"explicit"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 2nd component of the union, accessing `tag` results in `"generic"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"generic"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 3rd component of the union, accessing `tag` results in `"variadic"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"variadic"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 5th component of the union is `AstLocal` and it takes the 1st entry in the type pack is `AstExprGroup`, and `AstLocal` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersection as `Token<"@checked" | "@deprecated" | "@native">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"@checked" | "@deprecated" | "@native">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "attribute", location: span }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "attribute", location: span }` is not a supertype of `AstExprGroup`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:991:17-21: (W0) TypeError: Expected this to be
	'(Token<string>) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersection as `{ kind: "expr" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexName` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprAnonymousFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprUnary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprBinary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprInterpString` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTypeAssertion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIfElse` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` results in `"nil"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "nil" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "number", value: number }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGlobal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"..."` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "vararg" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprCall` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `{ kind: "stat" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstStatLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstStatFor` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFor` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstStatForIn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatForIn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstStatAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstStatCompoundAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatCompoundAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstStatFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstStatLocalFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocalFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstStatTypeAlias` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeAlias` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstStatTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstStatBlock` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatBlock` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstStatDo` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatDo` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstStatIf` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatIf` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstStatWhile` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatWhile` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstStatRepeat` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatRepeat` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` results in `"break"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "break" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "break" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersectiontext` results in `"continue"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "continue" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "continue" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union is `AstStatReturn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatReturn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstStatExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `{ kind: "type" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstTypeTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypeReference` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeReference` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstTypeTypeof` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTypeof` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstTypeGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstTypeUnion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeUnion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstTypeIntersection` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeIntersection` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"?"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "optional" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "optional" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstTypeArray` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeArray` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `{ kind: "typepack" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "typepack" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypePackExplicit` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackExplicit` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstTypePackGeneric` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackGeneric` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstTypePackVariadic` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackVariadic` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 5th component of the union is `AstLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"@checked"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"@native"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 3rd component of the union as `"@deprecated"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "attribute", location: span }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "attribute", location: span }` is not a supertype of `Token<string>`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:1019:9-20: (W0) TypeError: Type '(({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*' does not have key 'istoken'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:1020:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	nil | "nil" | "false" | "true" | string | "..." | "false" | "true" | "?" | "@checked" | "@deprecated" | "@native"
and at most:
	"nil" & "false" | "true" & string & "..." & "false" | "true" & "?" & "@checked" | "@deprecated" | "@native"
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:1021:9-24: (W0) TypeError: Type '(((AstExprAnonymousFunction | AstExprBinary | AstExprCall | AstExprGlobal | AstExprGroup | AstExprIfElse | AstExprIndexExpr | AstExprIndexName | AstExprInterpString | AstExprLocal | AstExprTable | AstExprTypeAssertion | AstExprUnary) & { read istoken: false?, kind: "expr" }) | ((AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { read istoken: false?, kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { read istoken: false?, kind: "typepack" })... *TRUNCATED*' does not have key 'istableitem'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/visitor.luau:1022:22-25: (W0) TypeError: Expected this to be
	'AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord'
but got
	'(((AstExprAnonymousFunction | AstExprBinary | AstExprCall | AstExprGlobal | AstExprGroup | AstExprIfElse | AstExprIndexExpr | AstExprIndexName | AstExprInterpString | AstExprLocal | AstExprTable | AstExprTypeAssertion | AstExprUnary) & { read istableitem: ~(false?), read istoken: false?, kind: "expr" }) | ((AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { read istableitem: ~(false?), read istoken: false?, kind: "type" })... *TRUNCATED*'; 
this is because 
	 * in the 1st component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` has the negation `"stat"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"stat"` is not a subtype of `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` results in `~"stat"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"stat"` is not a subtype of `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `kind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `kind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionkind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionkind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 4th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 4th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 4th component of the unionkind` results in `"local"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"local"` is not exactly `"list"`
	 * in the 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection, accessing `kind` has the negation `"local"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"local"` is not a subtype of `"list"`
	 * in the 2nd component of the intersection, accessing `kind` results in `~"local"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"local"` is not a subtype of `"list"`
	 * in the 3rd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 3rd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 3rd component of the intersection, accessing `kind` has the negation `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not a subtype of `"list"`
	 * in the 3rd component of the intersection, accessing `kind` results in `~"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"type"` is not a subtype of `"list"`
	 * in the 4th component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 4th component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 4th component of the intersection, accessing `kind` has the negation `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not a subtype of `"list"`
	 * in the 4th component of the intersection, accessing `kind` results in `~"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"expr"` is not a subtype of `"list"`
	 * in the 5th component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 5th component of the intersection, accessing `kind` has the negation `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not a subtype of `"list"`
	 * in the 5th component of the intersection, accessing `kind` results in `~"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"typepack"` is not a subtype of `"list"`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/init.luau:62:31-31: (W0) TypeError: Expected this to be
	'(AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord)?'
but got
	'{ read istableitem: ~(false?), location: span } & (({ read istableitem: ~(false?), kind: "expr" } & (AstExprGroup | (Token<"nil"> & { read istableitem: ~(false?), kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { read istableitem: ~(false?), kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { read istableitem: ~(false?), kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, read istableitem: ~(false?), kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" })... *TRUNCATED*'; 
this is because 
	 * in the 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection, accessing `istableitem` results in `~(false?)` in the latter type and `true` in the former type, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 1st component of the intersection as `{ kind: "expr" }`, and `boolean` is not exactly `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `boolean` is not exactly `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `boolean` is not exactly `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `boolean` is not exactly `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable`, and `boolean` is not exactly `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `boolean` is not exactly `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `boolean` is not exactly `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `boolean` is not exactly `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `boolean` is not exactly `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `boolean` is not exactly `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `boolean` is not exactly `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `boolean` is not exactly `Token<"nil">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `boolean` is not exactly `Token<"false" | "true">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `boolean` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `boolean` is not exactly `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `boolean` is not exactly `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `boolean` is not exactly `Token<"...">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 1st component of the intersection as `{ kind: "expr" }`, and `number` is not exactly `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `number` is not exactly `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `number` is not exactly `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `number` is not exactly `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `number` is not exactly `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `number` is not exactly `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `number` is not exactly `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `number` is not exactly `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `number` is not exactly `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `number` is not exactly `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `number` is not exactly `Token<"nil">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `number` is not exactly `Token<"false" | "true">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `number` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `number` is not exactly `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `number` is not exactly `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `number` is not exactly `Token<"...">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/init.luau:62:12-24: (W0) TypeError: Type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*' does not have key 'istableitem'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/init.luau:234:27-27: (W0) TypeError: Expected this to be
	'Token<string>?'
but got
	'{ read istoken: ~(false?), location: span } & (({ read istoken: ~(false?), kind: "expr" } & (AstExprGroup | (Token<"nil"> & { read istoken: ~(false?), kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { read istoken: ~(false?), kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { read istoken: ~(false?), kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, read istoken: ~(false?), kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal... *TRUNCATED*'; 
this is because 
	 * in the 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection, accessing `istoken` results in `~(false?)` in the latter type and `true` in the former type, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"` and in the 1st component of the union, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"` and in the 1st component of the union, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"` and in the 1st component of the union, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."` and in the 1st component of the union, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"` and in the 1st component of the union, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"` and in the 1st component of the union, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"` and in the 1st component of the union, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"` and in the 1st component of the union, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"` and in the 1st component of the union, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/init.luau:234:12-20: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/init.luau:238:12-20: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/query.luau:83:29-100: (W0) TypeError: Expected this to be
	'(Token<string>) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersection as `{ kind: "expr" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexName` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprAnonymousFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprUnary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprBinary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprInterpString` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTypeAssertion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIfElse` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` results in `"nil"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "nil" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "number", value: number }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGlobal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"..."` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "vararg" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprCall` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `{ kind: "stat" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstStatLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstStatFor` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFor` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstStatForIn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatForIn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstStatAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstStatCompoundAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatCompoundAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstStatFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstStatLocalFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocalFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstStatTypeAlias` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeAlias` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstStatTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstStatBlock` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatBlock` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstStatDo` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatDo` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstStatIf` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatIf` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstStatWhile` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatWhile` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstStatRepeat` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatRepeat` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` results in `"break"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "break" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "break" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersectiontext` results in `"continue"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "continue" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "continue" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union is `AstStatReturn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatReturn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstStatExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `{ kind: "type" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstTypeTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypeReference` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeReference` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstTypeTypeof` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTypeof` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstTypeGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstTypeUnion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeUnion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstTypeIntersection` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeIntersection` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"?"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "optional" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "optional" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstTypeArray` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeArray` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `{ kind: "typepack" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "typepack" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypePackExplicit` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackExplicit` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstTypePackGeneric` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackGeneric` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstTypePackVariadic` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackVariadic` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 5th component of the union is `AstLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"@checked"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"@native"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 3rd component of the union as `"@deprecated"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "attribute", location: span }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "attribute", location: span }` is not a supertype of `Token<string>`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/query.luau:136:24-31: (W0) TypeError: Key 'root' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "nil", text: "nil", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "boolean", text: "false" | "true", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: boolean }', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }', '{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "vararg", text: "...", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ istoken: true, kind: "stat", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "break", text: "break", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ istoken: true, kind: "stat", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "continue", text: "continue", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "boolean", text: "false" | "true", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: boolean }', '{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "optional", text: "?", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal', '{ istoken: true, kind: "attribute", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, text: "@checked" | "@deprecated" | "@native", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }' in the type 'ParseResult | ({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/query.luau:136:45-52: (W0) TypeError: Expected this to be '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', but got '~nil'; 
this is because 
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `nil` is not a subtype of `{ kind: "expr" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `nil` is not a subtype of `AstExprIndexName`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `nil` is not a subtype of `AstExprIndexExpr`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `nil` is not a subtype of `AstExprAnonymousFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `nil` is not a subtype of `AstExprTable`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `nil` is not a subtype of `AstExprUnary`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `nil` is not a subtype of `AstExprBinary`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `nil` is not a subtype of `AstExprInterpString`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `nil` is not a subtype of `AstExprTypeAssertion`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `nil` is not a subtype of `AstExprIfElse`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `nil` is not a subtype of `AstExprGroup`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `Token<"nil">`, and `nil` is not a subtype of `Token<"nil">`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `Token<"false" | "true">`, and `nil` is not a subtype of `Token<"false" | "true">`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `Token<string>`, and `nil` is not a subtype of `Token<string>`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 1st component of the intersection is `Token<string>`, and `nil` is not a subtype of `Token<string>`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `nil` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `nil` is not a subtype of `AstExprLocal`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `nil` is not a subtype of `AstExprGlobal`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection is `Token<"...">`, and `nil` is not a subtype of `Token<"...">`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `nil` is not a subtype of `AstExprCall`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `nil` is not a subtype of `{ kind: "stat" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `nil` is not a subtype of `AstStatLocal`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `nil` is not a subtype of `AstStatFor`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `nil` is not a subtype of `AstStatForIn`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `nil` is not a subtype of `AstStatAssign`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `nil` is not a subtype of `AstStatCompoundAssign`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `nil` is not a subtype of `AstStatFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `nil` is not a subtype of `AstStatLocalFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `nil` is not a subtype of `AstStatTypeAlias`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `nil` is not a subtype of `AstStatTypeFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `nil` is not a subtype of `AstStatBlock`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `nil` is not a subtype of `AstStatDo`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `nil` is not a subtype of `AstStatIf`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `nil` is not a subtype of `AstStatWhile`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `nil` is not a subtype of `AstStatRepeat`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection is `Token<"break">`, and `nil` is not a subtype of `Token<"break">`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `nil` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection is `Token<"continue">`, and `nil` is not a subtype of `Token<"continue">`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `nil` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `nil` is not a subtype of `AstStatReturn`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `nil` is not a subtype of `AstStatExpr`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `nil` is not a subtype of `{ kind: "type" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `nil` is not a subtype of `AstTypeTable`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `nil` is not a subtype of `AstTypeFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `nil` is not a subtype of `AstTypeReference`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `Token<"false" | "true">`, and `nil` is not a subtype of `Token<"false" | "true">`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `nil` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `Token<string>`, and `nil` is not a subtype of `Token<string>`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `nil` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `nil` is not a subtype of `AstTypeTypeof`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `nil` is not a subtype of `AstTypeGroup`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `nil` is not a subtype of `AstTypeUnion`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `nil` is not a subtype of `AstTypeIntersection`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection is `Token<"?">`, and `nil` is not a subtype of `Token<"?">`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `nil` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `nil` is not a subtype of `AstTypeArray`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `nil` is not a subtype of `{ kind: "typepack" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `nil` is not a subtype of `AstTypePackExplicit`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `nil` is not a subtype of `AstTypePackGeneric`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `nil` is not a subtype of `AstTypePackVariadic`
	 * the negation `nil` and in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `nil` is not a subtype of `AstLocal`
	 * the negation `nil` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection is `Token<"@checked" | "@deprecated" | "@native">`, and `nil` is not a subtype of `Token<"@checked" | "@deprecated" | "@native">`
	 * the negation `nil` and in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `nil` is not a subtype of `{ kind: "attribute", location: span }`
	 * the negation `nil` and the 1st component of the intersection is `{ location: span }`, and `nil` is not a subtype of `{ location: span }`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/query.luau:143:9-20: (W0) TypeError: Expected this to be
	'<U>(query<T>, (T) -> U?) -> *error-type*'
but got
	'<T, U>(query<T>, (T) -> U?) -> query<U>'; 
this is because 
	 * it returns the 1st entry in the type pack is `query<U>` in the latter type and `*error-type*` in the former type, and `query<U>` is not a subtype of `*error-type*`
	 * it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which is `U?` and it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which has the 1st component of the union as `U`, and `U?` is not a supertype of `U`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/query.luau:145:13-28: (W0) TypeError: Expected this to be
	'<U>(query<T>, ({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'<T, U>(query<T>, ({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it returns the 1st entry in the type pack is `query<U>` in the latter type and `*error-type*` in the former type, and `query<U>` is not a subtype of `*error-type*`
	 * it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which is `U?` and it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which has the 1st component of the union as `U`, and `U?` is not a supertype of `U`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/query.luau:147:16-34: (W0) TypeError: Expected this to be
	'<U>(query<T>, (T) -> U?) -> {U}'
but got
	'<T, U>(query<T>, (T) -> U?) -> {U}'; 
it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which is `U?` and it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which has the 1st component of the union as `U`, and `U?` is not a supertype of `U`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/trivia.luau:8:13-21: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/trivia.luau:7:38-100: (W0) TypeError: Expected this to be
	'query<Token<string>>'
but got
	'query<{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "nil", text: "nil", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} } | { annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location'; 
this is because 
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 1st component of the uniontext` results in `"nil"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 1st component of the union as `"@checked"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 2nd component of the union as `"@native"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 3rd component of the union as `"@deprecated"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 44th component of the uniontext` results in `"?"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 47th component of the uniontext` results in `"continue"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 48th component of the uniontext` results in `"break"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 49th component of the uniontext` results in `"..."` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/trivia.luau:31:13-21: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/utils/trivia.luau:30:38-100: (W0) TypeError: Expected this to be
	'query<Token<string>>'
but got
	'query<{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "nil", text: "nil", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} } | { annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location'; 
this is because 
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 1st component of the uniontext` results in `"nil"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 1st component of the union as `"@checked"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 2nd component of the union as `"@native"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 3rd component of the union as `"@deprecated"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 44th component of the uniontext` results in `"?"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 47th component of the uniontext` results in `"continue"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 48th component of the uniontext` results in `"break"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 49th component of the uniontext` results in `"..."` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`

./lute/std/libs/syntax/printer.luau:41:23-27: (W0) TypeError: Expected this to be
	'{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'Token<string>'; 
this is because 
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"`, and `string` is not exactly `"nil"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."`, and `string` is not exactly `"..."`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"`, and `string` is not exactly `"break"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"`, and `string` is not exactly `"continue"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"`, and `string` is not exactly `"?"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"@checked"`, and `string` is not exactly `"@checked"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"@native"`, and `string` is not exactly `"@native"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 3rd component of the union as `"@deprecated"`, and `string` is not exactly `"@deprecated"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `Token<string>` is not a subtype of `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `Token<string>` is not a subtype of `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `Token<string>` is not a subtype of `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `Token<string>` is not a subtype of `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `Token<string>` is not a subtype of `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `Token<string>` is not a subtype of `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `Token<string>` is not a subtype of `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `Token<string>` is not a subtype of `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `Token<string>` is not a subtype of `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `Token<string>` is not a subtype of `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `Token<string>` is not a subtype of `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `Token<string>` is not a subtype of `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `Token<string>` is not a subtype of `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `Token<string>` is not a subtype of `AstExprCall`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `Token<string>` is not a subtype of `{ kind: "stat" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `Token<string>` is not a subtype of `AstStatLocal`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `Token<string>` is not a subtype of `AstStatFor`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `Token<string>` is not a subtype of `AstStatForIn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `Token<string>` is not a subtype of `AstStatAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `Token<string>` is not a subtype of `AstStatCompoundAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `Token<string>` is not a subtype of `AstStatFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `Token<string>` is not a subtype of `AstStatLocalFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `Token<string>` is not a subtype of `AstStatTypeAlias`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `Token<string>` is not a subtype of `AstStatTypeFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `Token<string>` is not a subtype of `AstStatBlock`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `Token<string>` is not a subtype of `AstStatDo`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `Token<string>` is not a subtype of `AstStatIf`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `Token<string>` is not a subtype of `AstStatWhile`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `Token<string>` is not a subtype of `AstStatRepeat`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `Token<string>` is not a subtype of `AstStatReturn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `Token<string>` is not a subtype of `AstStatExpr`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `Token<string>` is not a subtype of `{ kind: "type" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `Token<string>` is not a subtype of `AstTypeTable`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `Token<string>` is not a subtype of `AstTypeFunction`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `Token<string>` is not a subtype of `AstTypeReference`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `Token<string>` is not a subtype of `AstTypeTypeof`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `Token<string>` is not a subtype of `AstTypeGroup`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `Token<string>` is not a subtype of `AstTypeUnion`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `Token<string>` is not a subtype of `AstTypeIntersection`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `Token<string>` is not a subtype of `AstTypeArray`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `Token<string>` is not a subtype of `{ kind: "typepack" }`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `Token<string>` is not a subtype of `AstTypePackExplicit`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `Token<string>` is not a subtype of `AstTypePackGeneric`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `Token<string>` is not a subtype of `AstTypePackVariadic`
	 * in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `Token<string>` is not a subtype of `AstLocal`
	 * in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `Token<string>` is not a subtype of `{ kind: "attribute", location: span }`

./lute/std/libs/syntax/printer.luau:42:50-54: (W0) TypeError: Expected this to be
	'{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'Token<string>'; 
this is because 
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"`, and `string` is not exactly `"nil"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."`, and `string` is not exactly `"..."`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"`, and `string` is not exactly `"break"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"`, and `string` is not exactly `"continue"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"`, and `string` is not exactly `"?"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"@checked"`, and `string` is not exactly `"@checked"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"@native"`, and `string` is not exactly `"@native"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 3rd component of the union as `"@deprecated"`, and `string` is not exactly `"@deprecated"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `Token<string>` is not a subtype of `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `Token<string>` is not a subtype of `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `Token<string>` is not a subtype of `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `Token<string>` is not a subtype of `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `Token<string>` is not a subtype of `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `Token<string>` is not a subtype of `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `Token<string>` is not a subtype of `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `Token<string>` is not a subtype of `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `Token<string>` is not a subtype of `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `Token<string>` is not a subtype of `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `Token<string>` is not a subtype of `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `Token<string>` is not a subtype of `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `Token<string>` is not a subtype of `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `Token<string>` is not a subtype of `AstExprCall`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `Token<string>` is not a subtype of `{ kind: "stat" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `Token<string>` is not a subtype of `AstStatLocal`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `Token<string>` is not a subtype of `AstStatFor`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `Token<string>` is not a subtype of `AstStatForIn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `Token<string>` is not a subtype of `AstStatAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `Token<string>` is not a subtype of `AstStatCompoundAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `Token<string>` is not a subtype of `AstStatFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `Token<string>` is not a subtype of `AstStatLocalFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `Token<string>` is not a subtype of `AstStatTypeAlias`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `Token<string>` is not a subtype of `AstStatTypeFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `Token<string>` is not a subtype of `AstStatBlock`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `Token<string>` is not a subtype of `AstStatDo`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `Token<string>` is not a subtype of `AstStatIf`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `Token<string>` is not a subtype of `AstStatWhile`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `Token<string>` is not a subtype of `AstStatRepeat`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `Token<string>` is not a subtype of `AstStatReturn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `Token<string>` is not a subtype of `AstStatExpr`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `Token<string>` is not a subtype of `{ kind: "type" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `Token<string>` is not a subtype of `AstTypeTable`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `Token<string>` is not a subtype of `AstTypeFunction`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `Token<string>` is not a subtype of `AstTypeReference`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `Token<string>` is not a subtype of `AstTypeTypeof`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `Token<string>` is not a subtype of `AstTypeGroup`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `Token<string>` is not a subtype of `AstTypeUnion`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `Token<string>` is not a subtype of `AstTypeIntersection`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `Token<string>` is not a subtype of `AstTypeArray`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `Token<string>` is not a subtype of `{ kind: "typepack" }`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `Token<string>` is not a subtype of `AstTypePackExplicit`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `Token<string>` is not a subtype of `AstTypePackGeneric`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `Token<string>` is not a subtype of `AstTypePackVariadic`
	 * in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `Token<string>` is not a subtype of `AstLocal`
	 * in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `Token<string>` is not a subtype of `{ kind: "attribute", location: span }`

./lute/std/libs/syntax/printer.luau:42:25-29: (W0) TypeError: Expected this to be
	'{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'Token<string>'; 
this is because 
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"`, and `string` is not exactly `"nil"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."`, and `string` is not exactly `"..."`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"`, and `string` is not exactly `"break"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"`, and `string` is not exactly `"continue"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"`, and `string` is not exactly `"?"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"@checked"`, and `string` is not exactly `"@checked"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"@native"`, and `string` is not exactly `"@native"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 3rd component of the union as `"@deprecated"`, and `string` is not exactly `"@deprecated"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `Token<string>` is not a subtype of `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `Token<string>` is not a subtype of `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `Token<string>` is not a subtype of `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `Token<string>` is not a subtype of `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `Token<string>` is not a subtype of `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `Token<string>` is not a subtype of `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `Token<string>` is not a subtype of `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `Token<string>` is not a subtype of `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `Token<string>` is not a subtype of `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `Token<string>` is not a subtype of `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `Token<string>` is not a subtype of `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `Token<string>` is not a subtype of `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `Token<string>` is not a subtype of `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `Token<string>` is not a subtype of `AstExprCall`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `Token<string>` is not a subtype of `{ kind: "stat" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `Token<string>` is not a subtype of `AstStatLocal`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `Token<string>` is not a subtype of `AstStatFor`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `Token<string>` is not a subtype of `AstStatForIn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `Token<string>` is not a subtype of `AstStatAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `Token<string>` is not a subtype of `AstStatCompoundAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `Token<string>` is not a subtype of `AstStatFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `Token<string>` is not a subtype of `AstStatLocalFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `Token<string>` is not a subtype of `AstStatTypeAlias`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `Token<string>` is not a subtype of `AstStatTypeFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `Token<string>` is not a subtype of `AstStatBlock`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `Token<string>` is not a subtype of `AstStatDo`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `Token<string>` is not a subtype of `AstStatIf`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `Token<string>` is not a subtype of `AstStatWhile`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `Token<string>` is not a subtype of `AstStatRepeat`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `Token<string>` is not a subtype of `AstStatReturn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `Token<string>` is not a subtype of `AstStatExpr`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `Token<string>` is not a subtype of `{ kind: "type" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `Token<string>` is not a subtype of `AstTypeTable`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `Token<string>` is not a subtype of `AstTypeFunction`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `Token<string>` is not a subtype of `AstTypeReference`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `Token<string>` is not a subtype of `AstTypeTypeof`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `Token<string>` is not a subtype of `AstTypeGroup`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `Token<string>` is not a subtype of `AstTypeUnion`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `Token<string>` is not a subtype of `AstTypeIntersection`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `Token<string>` is not a subtype of `AstTypeArray`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `Token<string>` is not a subtype of `{ kind: "typepack" }`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `Token<string>` is not a subtype of `AstTypePackExplicit`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `Token<string>` is not a subtype of `AstTypePackGeneric`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `Token<string>` is not a subtype of `AstTypePackVariadic`
	 * in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `Token<string>` is not a subtype of `AstLocal`
	 * in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `Token<string>` is not a subtype of `{ kind: "attribute", location: span }`

./lute/std/libs/syntax/printer.luau:203:2-19: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"" | nil
and at most:
	""
but these types are not compatible with one another.

./lute/std/libs/syntax/query.luau:83:29-100: (W0) TypeError: Expected this to be
	'(Token<string>) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersection as `{ kind: "expr" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexName` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprAnonymousFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprUnary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprBinary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprInterpString` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTypeAssertion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIfElse` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` results in `"nil"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "nil" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "number", value: number }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGlobal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"..."` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "vararg" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprCall` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `{ kind: "stat" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstStatLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstStatFor` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFor` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstStatForIn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatForIn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstStatAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstStatCompoundAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatCompoundAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstStatFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstStatLocalFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocalFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstStatTypeAlias` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeAlias` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstStatTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstStatBlock` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatBlock` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstStatDo` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatDo` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstStatIf` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatIf` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstStatWhile` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatWhile` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstStatRepeat` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatRepeat` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` results in `"break"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "break" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "break" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersectiontext` results in `"continue"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "continue" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "continue" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union is `AstStatReturn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatReturn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstStatExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `{ kind: "type" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstTypeTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypeReference` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeReference` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstTypeTypeof` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTypeof` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstTypeGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstTypeUnion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeUnion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstTypeIntersection` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeIntersection` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"?"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "optional" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "optional" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstTypeArray` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeArray` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `{ kind: "typepack" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "typepack" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypePackExplicit` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackExplicit` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstTypePackGeneric` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackGeneric` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstTypePackVariadic` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackVariadic` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 5th component of the union is `AstLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"@checked"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"@native"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 3rd component of the union as `"@deprecated"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "attribute", location: span }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "attribute", location: span }` is not a supertype of `Token<string>`

./lute/std/libs/syntax/query.luau:136:24-31: (W0) TypeError: Key 'root' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "nil", text: "nil", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "boolean", text: "false" | "true", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: boolean }', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }', '{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "vararg", text: "...", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ istoken: true, kind: "stat", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "break", text: "break", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ istoken: true, kind: "stat", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "continue", text: "continue", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "boolean", text: "false" | "true", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: boolean }', '{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "optional", text: "?", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal', '{ istoken: true, kind: "attribute", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, text: "@checked" | "@deprecated" | "@native", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }' in the type 'ParseResult | ({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

./lute/std/libs/syntax/query.luau:136:45-52: (W0) TypeError: Expected this to be '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', but got '~nil'; 
this is because 
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `nil` is not a subtype of `{ kind: "expr" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `nil` is not a subtype of `AstExprIndexName`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `nil` is not a subtype of `AstExprIndexExpr`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `nil` is not a subtype of `AstExprAnonymousFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `nil` is not a subtype of `AstExprTable`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `nil` is not a subtype of `AstExprUnary`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `nil` is not a subtype of `AstExprBinary`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `nil` is not a subtype of `AstExprInterpString`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `nil` is not a subtype of `AstExprTypeAssertion`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `nil` is not a subtype of `AstExprIfElse`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `nil` is not a subtype of `AstExprGroup`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `Token<"nil">`, and `nil` is not a subtype of `Token<"nil">`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `Token<"false" | "true">`, and `nil` is not a subtype of `Token<"false" | "true">`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `Token<string>`, and `nil` is not a subtype of `Token<string>`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 1st component of the intersection is `Token<string>`, and `nil` is not a subtype of `Token<string>`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `nil` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `nil` is not a subtype of `AstExprLocal`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `nil` is not a subtype of `AstExprGlobal`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection is `Token<"...">`, and `nil` is not a subtype of `Token<"...">`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `nil` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `nil` is not a subtype of `AstExprCall`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `nil` is not a subtype of `{ kind: "stat" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `nil` is not a subtype of `AstStatLocal`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `nil` is not a subtype of `AstStatFor`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `nil` is not a subtype of `AstStatForIn`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `nil` is not a subtype of `AstStatAssign`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `nil` is not a subtype of `AstStatCompoundAssign`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `nil` is not a subtype of `AstStatFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `nil` is not a subtype of `AstStatLocalFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `nil` is not a subtype of `AstStatTypeAlias`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `nil` is not a subtype of `AstStatTypeFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `nil` is not a subtype of `AstStatBlock`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `nil` is not a subtype of `AstStatDo`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `nil` is not a subtype of `AstStatIf`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `nil` is not a subtype of `AstStatWhile`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `nil` is not a subtype of `AstStatRepeat`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection is `Token<"break">`, and `nil` is not a subtype of `Token<"break">`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `nil` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection is `Token<"continue">`, and `nil` is not a subtype of `Token<"continue">`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `nil` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `nil` is not a subtype of `AstStatReturn`
	 * the negation `nil` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `nil` is not a subtype of `AstStatExpr`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `nil` is not a subtype of `{ kind: "type" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `nil` is not a subtype of `AstTypeTable`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `nil` is not a subtype of `AstTypeFunction`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `nil` is not a subtype of `AstTypeReference`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `Token<"false" | "true">`, and `nil` is not a subtype of `Token<"false" | "true">`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `nil` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `Token<string>`, and `nil` is not a subtype of `Token<string>`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `nil` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `nil` is not a subtype of `AstTypeTypeof`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `nil` is not a subtype of `AstTypeGroup`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `nil` is not a subtype of `AstTypeUnion`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `nil` is not a subtype of `AstTypeIntersection`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection is `Token<"?">`, and `nil` is not a subtype of `Token<"?">`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `nil` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `nil` is not a subtype of `AstTypeArray`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `nil` is not a subtype of `{ kind: "typepack" }`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `nil` is not a subtype of `AstTypePackExplicit`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `nil` is not a subtype of `AstTypePackGeneric`
	 * the negation `nil` and in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `nil` is not a subtype of `AstTypePackVariadic`
	 * the negation `nil` and in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `nil` is not a subtype of `AstLocal`
	 * the negation `nil` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection is `Token<"@checked" | "@deprecated" | "@native">`, and `nil` is not a subtype of `Token<"@checked" | "@deprecated" | "@native">`
	 * the negation `nil` and in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `nil` is not a subtype of `{ kind: "attribute", location: span }`
	 * the negation `nil` and the 1st component of the intersection is `{ location: span }`, and `nil` is not a subtype of `{ location: span }`

./lute/std/libs/syntax/query.luau:143:9-20: (W0) TypeError: Expected this to be
	'<U>(query<T>, (T) -> U?) -> *error-type*'
but got
	'<T, U>(query<T>, (T) -> U?) -> query<U>'; 
this is because 
	 * it returns the 1st entry in the type pack is `query<U>` in the latter type and `*error-type*` in the former type, and `query<U>` is not a subtype of `*error-type*`
	 * it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which is `U?` and it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which has the 1st component of the union as `U`, and `U?` is not a supertype of `U`

./lute/std/libs/syntax/query.luau:145:13-28: (W0) TypeError: Expected this to be
	'<U>(query<T>, ({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'<T, U>(query<T>, ({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it returns the 1st entry in the type pack is `query<U>` in the latter type and `*error-type*` in the former type, and `query<U>` is not a subtype of `*error-type*`
	 * it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which is `U?` and it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which has the 1st component of the union as `U`, and `U?` is not a supertype of `U`

./lute/std/libs/syntax/query.luau:147:16-34: (W0) TypeError: Expected this to be
	'<U>(query<T>, (T) -> U?) -> {U}'
but got
	'<T, U>(query<T>, (T) -> U?) -> {U}'; 
it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which is `U?` and it takes the 2nd entry in the type pack, the function returns the 1st entry in the type pack which has the 1st component of the union as `U`, and `U?` is not a supertype of `U`

./lute/std/libs/syntax/utils/init.luau:62:31-31: (W0) TypeError: Expected this to be
	'(AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord)?'
but got
	'{ read istableitem: ~(false?), location: span } & (({ read istableitem: ~(false?), kind: "expr" } & (AstExprGroup | (Token<"nil"> & { read istableitem: ~(false?), kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { read istableitem: ~(false?), kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { read istableitem: ~(false?), kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, read istableitem: ~(false?), kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" })... *TRUNCATED*'; 
this is because 
	 * in the 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection, accessing `istableitem` results in `~(false?)` in the latter type and `true` in the former type, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 1st component of the intersection as `{ kind: "expr" }`, and `boolean` is not exactly `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `boolean` is not exactly `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `boolean` is not exactly `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `boolean` is not exactly `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable`, and `boolean` is not exactly `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `boolean` is not exactly `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `boolean` is not exactly `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `boolean` is not exactly `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `boolean` is not exactly `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `boolean` is not exactly `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `boolean` is not exactly `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `boolean` is not exactly `Token<"nil">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `boolean` is not exactly `Token<"false" | "true">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `boolean` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `boolean` is not exactly `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `boolean` is not exactly `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `boolean` is not exactly `Token<"...">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `value` results in `boolean` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 1st component of the intersection as `{ kind: "expr" }`, and `number` is not exactly `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `number` is not exactly `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `number` is not exactly `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `number` is not exactly `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `number` is not exactly `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `number` is not exactly `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `number` is not exactly `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `number` is not exactly `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `number` is not exactly `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `number` is not exactly `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `number` is not exactly `Token<"nil">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `number` is not exactly `Token<"false" | "true">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `number` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `number` is not exactly `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `number` is not exactly `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `number` is not exactly `Token<"...">`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection, accessing `value` results in `number` and in the 1st component of the union, accessing `value` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`

./lute/std/libs/syntax/utils/init.luau:62:12-24: (W0) TypeError: Type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*' does not have key 'istableitem'

./lute/std/libs/syntax/utils/init.luau:234:27-27: (W0) TypeError: Expected this to be
	'Token<string>?'
but got
	'{ read istoken: ~(false?), location: span } & (({ read istoken: ~(false?), kind: "expr" } & (AstExprGroup | (Token<"nil"> & { read istoken: ~(false?), kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { read istoken: ~(false?), kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { read istoken: ~(false?), kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, read istoken: ~(false?), kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal... *TRUNCATED*'; 
this is because 
	 * in the 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection, accessing `istoken` results in `~(false?)` in the latter type and `true` in the former type, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"` and in the 1st component of the union, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"` and in the 1st component of the union, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"` and in the 1st component of the union, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."` and in the 1st component of the union, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"` and in the 1st component of the union, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"` and in the 1st component of the union, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"` and in the 1st component of the union, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"` and in the 1st component of the union, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"` and in the 1st component of the union, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection, accessing `istoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` has the negation `false?` and in the 1st component of the union, accessing `istoken` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the unionistoken` results in `~(false?)` and in the 1st component of the union, accessing `istoken` results in `true`, and `~(false?)` is not a subtype of `true`

./lute/std/libs/syntax/utils/init.luau:234:12-20: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

./lute/std/libs/syntax/utils/init.luau:238:12-20: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

./lute/std/libs/syntax/utils/trivia.luau:8:13-21: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

./lute/std/libs/syntax/utils/trivia.luau:7:38-100: (W0) TypeError: Expected this to be
	'query<Token<string>>'
but got
	'query<{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "nil", text: "nil", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} } | { annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location'; 
this is because 
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 1st component of the uniontext` results in `"nil"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 1st component of the union as `"@checked"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 2nd component of the union as `"@native"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 3rd component of the union as `"@deprecated"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 44th component of the uniontext` results in `"?"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 47th component of the uniontext` results in `"continue"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 48th component of the uniontext` results in `"break"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 49th component of the uniontext` results in `"..."` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`

./lute/std/libs/syntax/utils/trivia.luau:31:13-21: (W0) TypeError: Key 'istoken' is missing from '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }', '{ kind: "expr", location: span, name: Token<string>, tag: "global" }', '{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "expr", location: span, tag: "function" }', '{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }', '{ kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, tag: "block" }', '{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, kind: "stat", ... *TRUNCATED*', '{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">?, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ equals: Token<"=">, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }', '{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }', '{ closeparameters: Token<">">?, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*', '{ closeparens: Token<")">, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & ... *TRUNCATED*', '{ kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }... *TRUNCATED*', '{ access: Token<"read" | "write">?, closebrace: Token<"}">, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }', '{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*', '{ closeparens: Token<")">?, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*', '{ ellipsis: Token<"...">, kind: "typepack", location: span, name: Token<string>, tag: "generic" }', '{ ellipsis: Token<"...">?, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" } ... *TRUNCATED*', 'AstLocal' in the type '{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'

./lute/std/libs/syntax/utils/trivia.luau:30:38-100: (W0) TypeError: Expected this to be
	'query<Token<string>>'
but got
	'query<{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "nil", text: "nil", trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} } | { annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location'; 
this is because 
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 1st component of the uniontext` results in `"nil"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 1st component of the union as `"@checked"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 2nd component of the union as `"@native"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 43rd component of the uniontext` has the 3rd component of the union as `"@deprecated"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 44th component of the uniontext` results in `"?"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 46th component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 47th component of the uniontext` results in `"continue"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 48th component of the uniontext` results in `"break"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 49th component of the uniontext` results in `"..."` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 1st component of the union as `"true"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 52nd component of the uniontext` has the 2nd component of the union as `"false"` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `filter`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 10th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"|">?, location: span, tag: "union", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 11th component of the union as `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, read istoken: ~false & ~nil, kind: "type", location: span, openparens: Token<"(">, tag: "group", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 12th component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 13th component of the union as `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparameters: Token<">">?, read istoken: ~false & ~nil, kind: "type", location: span, name: Token<string>, openparameters: Token<"<">?, parameters: Punctuated<(((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 14th component of the union as `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstFunctionBody, export: Token<"export">?, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, tag: "typefunction", type: Token<"type"> }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 15th component of the union as `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, equals: Token<"=">, export: Token<"export">?, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "stat", location: span, name: Token<string>, opengenerics: Token<"<">?, tag: "typealias", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 16th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, name: AstLocal, tag: "localfunction" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 17th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "stat", location: span, name: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 18th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, operand: Token<string>, tag: "compoundassign", value: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 19th component of the union as `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "assign", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 20th component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, forkeyword: Token<"for">, inkeyword: Token<"in">, read istoken: ~false & ~nil, kind: "stat", location: span, tag: "forin", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 21st component of the union as `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, dokeyword: Token<"do">, endkeyword: Token<"end">, equals: Token<"=">, forkeyword: Token<"for">, from: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 22nd component of the union as `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ equals: Token<"=">?, read istoken: ~false & ~nil, kind: "stat", localkeyword: Token<"local">, location: span, tag: "local", values: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 23rd component of the union as `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 24th component of the union as `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 25th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 26th component of the union as `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: AstStatBlock, condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 27th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 28th component of the union as `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ body: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }}, dokeyword: Token<"do">, endkeyword: Token<"end">, read istoken: ... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 29th component of the union as `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "stat", location: span, statements: {((Token<"break"> & { kind: "stat", location: span, tag: "break" }) | (Token<"continue"> & { kind: "stat", location: span, tag: "continue" }) | AstStatAssign | AstStatBlock | AstStatCompoundAssign | AstStatDo | AstStatExpr | AstStatFor | AstStatForIn | AstStatFunction | AstStatIf | AstStatLocal | AstStatLocalFunction | AstStatRepeat | AstStatReturn | AstStatTypeAlias | AstStatTypeFunction | AstStatWhile) & { kind: "stat" }},... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 2nd component of the union as `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: (((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" })?, colon: Token<":">?, read istoken: ~false & ~nil, kind: "local", location... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 30th component of the union as `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ condition: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 31st component of the union as `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ annotation: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { kind: "type" }, read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" }... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 32nd component of the union as `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ expressions: {{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 33rd component of the union as `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", lhsoperand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 34th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, operand: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 35th component of the union as `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord}, read istoken: ~false & ~nil, kind: "expr", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 36th component of the union as `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ attributes: {Token<"@checked" | "@deprecated" | "@native"> & { kind: "attribute", location: span }}, body: AstFunctionBody, functionkeyword: Token<"function">, read istoken: ~false & ~nil, kind: "expr", location: span, tag: "function" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 37th component of the union as `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrackets: Token<"]">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 38th component of the union as `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ accessor: Token<"." | ":">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 39th component of the union as `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ argLocation: span, arguments: Punctuated<{ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 3rd component of the union as `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">?, read istoken: ~false & ~nil, kind: "typepack", location: span, tag: "variadic", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 40th component of the union as `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", location: span, name: Token<string>, tag: "global" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 41st component of the union as `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "expr", local: AstLocal, location: span, tag: "local", token: Token<string>, upvalue: boolean }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 42nd component of the union as `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">, expression: { kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 45th component of the union as `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "type", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "double" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 4th component of the union as `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ ellipsis: Token<"...">, read istoken: ~false & ~nil, kind: "typepack", location: span, name: Token<string>, tag: "generic" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 50th component of the union as `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ blockdepth: number, istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace} }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 51st component of the union as `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ istoken: true, kind: "expr", leadingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, location: span, tag: "number", text: string, trailingtrivia: {MultiLineComment | SingleLineComment | Whitespace}, value: number }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 5th component of the union as `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closeparens: Token<")">?, read istoken: ~false & ~nil, kind: "typepack", location: span, openparens: Token<"(">?, tag: "explicit", tailtype: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })?, types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 6th component of the union as `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closegenerics: Token<">">?, closeparens: Token<")">, genericpacks: Punctuated<AstGenericTypePack, ",">?, generics: Punctuated<AstGenericType, ",">?, read istoken: ~false & ~nil, kind: "type", location: span, opengenerics: Token<"<">?, openparens: Token<"(">, parameters: Punctuated<AstTypeFunctionParameter, ",">, returnarrow: Token<"->">, returntypes: (AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }, tag: "function", vararg: ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" })... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 7th component of the union as `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ closebrace: Token<"}">, entries: {AstTypeTableItemIndexer | AstTypeTableItemProperty | AstTypeTableItemStringProperty}, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "table" }` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 8th component of the union as `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ access: Token<"read" | "write">?, closebrace: Token<"}">, read istoken: ~false & ~nil, kind: "type", location: span, openbrace: Token<"\123">, tag: "array", type: ((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`
	 * accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which has the 9th component of the union as `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` and accessing `filter`, takes the 1st entry in the type pack, accessing `flatmap`, takes the 2nd entry in the type pack, the function takes the 1st entry in the type pack which is `Token<string>`, and `{ read istoken: ~false & ~nil, kind: "type", leading: Token<"&">?, location: span, tag: "intersection", types: Punctuated<((Token<"?"> & { kind: "type", location: span, tag: "optional" }) | (Token<"false" | "true"> & { kind: "type", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }) | AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion)... *TRUNCATED*` is not exactly `Token<string>`

./lute/std/libs/syntax/visitor.luau:239:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"break" | nil
and at most:
	"break"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:245:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"continue" | nil
and at most:
	"continue"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:376:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"nil" | nil
and at most:
	"nil"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:382:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"false" | "true" | nil
and at most:
	"false" | "true"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:388:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	string | nil
and at most:
	string
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:406:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"..." | nil
and at most:
	"..."
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:475:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"@checked" | "@deprecated" | "@native" | nil
and at most:
	"@checked" | "@deprecated" | "@native"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:650:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"false" | "true" | nil
and at most:
	"false" | "true"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:656:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	string | nil
and at most:
	string
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:679:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"?" | nil
and at most:
	"?"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:938:24-28: (W0) TypeError: Expected this to be
	'(AstStatBlock) -> ()'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
it returns `boolean` in the latter type and `()` in the former type, and `boolean` is not a subtype of `()`

./lute/std/libs/syntax/visitor.luau:947:35-39: (W0) TypeError: Expected this to be
	'(AstStatLocal) -> ()'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
it returns `boolean` in the latter type and `()` in the former type, and `boolean` is not a subtype of `()`

./lute/std/libs/syntax/visitor.luau:959:19-23: (W0) TypeError: Expected this to be
	'({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
it returns `boolean` in the latter type and `()` in the former type, and `boolean` is not a subtype of `()`

./lute/std/libs/syntax/visitor.luau:966:25-29: (W0) TypeError: Expected this to be
	'(AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 12th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 13th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 14th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 15th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 16th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 17th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 18th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `AstExprGroup` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"nil">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"false" | "true">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 1st component of the intersection as `{ kind: "expr" }`, and `boolean` is not exactly `{ kind: "expr" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `boolean` is not exactly `AstExprIndexName`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `boolean` is not exactly `AstExprIndexExpr`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `boolean` is not exactly `AstExprAnonymousFunction`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable`, and `boolean` is not exactly `AstExprTable`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `boolean` is not exactly `AstExprUnary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `boolean` is not exactly `AstExprBinary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `boolean` is not exactly `AstExprInterpString`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `boolean` is not exactly `AstExprTypeAssertion`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `boolean` is not exactly `AstExprIfElse`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `boolean` is not exactly `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `boolean` is not exactly `Token<"nil">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `boolean` is not exactly `Token<"false" | "true">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `boolean` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `boolean` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `boolean` is not exactly `AstExprLocal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `boolean` is not exactly `AstExprGlobal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `boolean` is not exactly `Token<"...">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `boolean` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionvalue` results in `boolean` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall`, and `boolean` is not exactly `AstExprCall`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<string>` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 1st component of the intersection as `{ kind: "expr" }`, and `number` is not exactly `{ kind: "expr" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName`, and `number` is not exactly `AstExprIndexName`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr`, and `number` is not exactly `AstExprIndexExpr`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction`, and `number` is not exactly `AstExprAnonymousFunction`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable`, and `number` is not exactly `AstExprTable`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary`, and `number` is not exactly `AstExprUnary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary`, and `number` is not exactly `AstExprBinary`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString`, and `number` is not exactly `AstExprInterpString`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion`, and `number` is not exactly `AstExprTypeAssertion`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse`, and `number` is not exactly `AstExprIfElse`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup`, and `number` is not exactly `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"nil">`, and `number` is not exactly `Token<"nil">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "nil" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<"false" | "true">`, and `number` is not exactly `Token<"false" | "true">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "number", value: number }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>`, and `number` is not exactly `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `number` is not exactly `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal`, and `number` is not exactly `AstExprLocal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal`, and `number` is not exactly `AstExprGlobal`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">`, and `number` is not exactly `Token<"...">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }`, and `number` is not exactly `{ kind: "expr", location: span, tag: "vararg" }`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionvalue` results in `number` and it takes the 1st entry in the type pack has the 1st component of the unionvalue` has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall`, and `number` is not exactly `AstExprCall`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 1st component of the intersection as `Token<string>` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<string>` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 6th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 7th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"...">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"...">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersectionkind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `kind` results in `"expr"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectionkind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersection as `Token<"break">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"break">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersectionkind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersection as `Token<"continue">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"continue">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersectionkind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `kind` results in `"stat"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"stat"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectionkind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `kind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"false" | "true">` and it takes the 1st entry in the type pack has the 1st component of the union as `AstExprTableItemList`, and `Token<"false" | "true">` is not a supertype of `AstExprTableItemList`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionkind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionkind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `kind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union, accessing `kind` results in `"type"` and it takes the 1st entry in the type pack has the 1st component of the unionkind` results in `"list"`, and `"type"` is not exactly `"list"`

./lute/std/libs/syntax/visitor.luau:970:21-25: (W0) TypeError: Expected this to be
	'(AstExprGroup) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersection as `{ kind: "expr" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "expr" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `{ kind: "stat" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "stat" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `tag` results in `"local"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"local"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `tag` results in `"for"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"for"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union, accessing `tag` results in `"forin"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"forin"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union, accessing `tag` results in `"assign"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"assign"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union, accessing `tag` results in `"compoundassign"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"compoundassign"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union, accessing `tag` results in `"function"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"function"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union, accessing `tag` results in `"localfunction"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"localfunction"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union, accessing `tag` results in `"typealias"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"typealias"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union, accessing `tag` results in `"typefunction"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"typefunction"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `tag` results in `"block"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"block"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union, accessing `tag` results in `"do"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"do"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union, accessing `tag` results in `"conditional"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"conditional"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `tag` results in `"while"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"while"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union, accessing `tag` results in `"repeat"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"repeat"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersection as `Token<"break">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"break">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersectiontag` results in `"break"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"break"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersection as `Token<"continue">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"continue">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersectiontag` results in `"continue"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"continue"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union, accessing `tag` results in `"return"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"return"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `tag` results in `"expression"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"expression"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `{ kind: "type" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "type" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 10th component of the union, accessing `tag` results in `"table"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"table"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 11th component of the union, accessing `tag` results in `"function"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"function"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `tag` results in `"reference"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"reference"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `Token<"false" | "true">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"false" | "true">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectiontag` results in `"boolean"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"boolean"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `Token<string>` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<string>` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectiontag` results in `"string"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"string"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union, accessing `tag` results in `"typeof"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"typeof"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstTypeGroup` and it takes the 1st entry in the type pack is `AstExprGroup`, and `AstTypeGroup` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 6th component of the union, accessing `tag` results in `"union"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"union"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union, accessing `tag` results in `"intersection"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"intersection"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersection as `Token<"?">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"?">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersectiontag` results in `"optional"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"optional"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 9th component of the union, accessing `tag` results in `"array"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"array"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `{ kind: "typepack" }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "typepack" }` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `closeparens` has the 2nd component of the union as `nil` and it takes the 1st entry in the type pack, accessing `closeparens` results in `Token<")">`, and `nil` is not exactly `Token<")">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `openparens` has the 2nd component of the union as `nil` and it takes the 1st entry in the type pack, accessing `openparens` results in `Token<"(">`, and `nil` is not exactly `Token<"(">`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union, accessing `tag` results in `"explicit"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"explicit"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 2nd component of the union, accessing `tag` results in `"generic"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"generic"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 3rd component of the union, accessing `tag` results in `"variadic"` and it takes the 1st entry in the type pack, accessing `tag` results in `"group"`, and `"variadic"` is not exactly `"group"`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 5th component of the union is `AstLocal` and it takes the 1st entry in the type pack is `AstExprGroup`, and `AstLocal` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersection as `Token<"@checked" | "@deprecated" | "@native">` and it takes the 1st entry in the type pack is `AstExprGroup`, and `Token<"@checked" | "@deprecated" | "@native">` is not a supertype of `AstExprGroup`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "attribute", location: span }` and it takes the 1st entry in the type pack is `AstExprGroup`, and `{ kind: "attribute", location: span }` is not a supertype of `AstExprGroup`

./lute/std/libs/syntax/visitor.luau:991:17-21: (W0) TypeError: Expected this to be
	'(Token<string>) -> boolean'
but got
	'({ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'; 
this is because 
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 1st component of the intersection as `{ kind: "expr" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstExprIndexName` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexName` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstExprIndexExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIndexExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstExprAnonymousFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprAnonymousFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstExprTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstExprUnary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprUnary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstExprBinary` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprBinary` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstExprInterpString` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprInterpString` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstExprTypeAssertion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprTypeAssertion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstExprIfElse` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprIfElse` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstExprGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` results in `"nil"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"nil"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "nil" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "nil" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "number", value: number }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "number", value: number }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 5th component of the union has the 2nd component of the intersection as `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstExprLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstExprGlobal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprGlobal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"..."` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"..."` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "expr", location: span, tag: "vararg" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "expr", location: span, tag: "vararg" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 1st component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstExprCall` and it takes the 1st entry in the type pack is `Token<string>`, and `AstExprCall` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersection as `{ kind: "stat" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstStatLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstStatFor` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFor` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 12th component of the union is `AstStatForIn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatForIn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 13th component of the union is `AstStatAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 14th component of the union is `AstStatCompoundAssign` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatCompoundAssign` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 15th component of the union is `AstStatFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 16th component of the union is `AstStatLocalFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatLocalFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 17th component of the union is `AstStatTypeAlias` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeAlias` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 18th component of the union is `AstStatTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstStatBlock` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatBlock` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstStatDo` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatDo` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstStatIf` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatIf` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstStatWhile` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatWhile` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstStatRepeat` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatRepeat` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` results in `"break"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"break"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "break" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "break" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 1st component of the intersectiontext` results in `"continue"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"continue"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 7th component of the union has the 2nd component of the intersection as `{ kind: "stat", location: span, tag: "continue" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "stat", location: span, tag: "continue" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 8th component of the union is `AstStatReturn` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatReturn` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstStatExpr` and it takes the 1st entry in the type pack is `Token<string>`, and `AstStatExpr` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 1st component of the intersection as `{ kind: "type" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 10th component of the union is `AstTypeTable` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTable` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 11th component of the union is `AstTypeFunction` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeFunction` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypeReference` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeReference` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"true"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"true"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"false"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"false"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 2nd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "boolean", value: boolean }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "boolean", value: boolean }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 4th component of the union is `AstTypeTypeof` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeTypeof` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 5th component of the union is `AstTypeGroup` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeGroup` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 6th component of the union is `AstTypeUnion` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeUnion` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 7th component of the union is `AstTypeIntersection` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeIntersection` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 1st component of the intersectiontext` results in `"?"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"?"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 8th component of the union has the 2nd component of the intersection as `{ kind: "type", location: span, tag: "optional" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "type", location: span, tag: "optional" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 3rd component of the union has the 2nd component of the intersectionthe 9th component of the union is `AstTypeArray` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypeArray` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 1st component of the intersection as `{ kind: "typepack" }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "typepack" }` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 1st component of the union is `AstTypePackExplicit` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackExplicit` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 2nd component of the union is `AstTypePackGeneric` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackGeneric` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 4th component of the union has the 2nd component of the intersectionthe 3rd component of the union is `AstTypePackVariadic` and it takes the 1st entry in the type pack is `Token<string>`, and `AstTypePackVariadic` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 5th component of the union is `AstLocal` and it takes the 1st entry in the type pack is `Token<string>`, and `AstLocal` is not a supertype of `Token<string>`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 1st component of the union as `"@checked"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@checked"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 2nd component of the union as `"@native"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@native"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 1st component of the intersectiontext` has the 3rd component of the union as `"@deprecated"` and it takes the 1st entry in the type pack, accessing `text` results in `string`, and `"@deprecated"` is not exactly `string`
	 * it takes the 1st entry in the type pack has the 2nd component of the intersectionthe 6th component of the union has the 2nd component of the intersection as `{ kind: "attribute", location: span }` and it takes the 1st entry in the type pack is `Token<string>`, and `{ kind: "attribute", location: span }` is not a supertype of `Token<string>`

./lute/std/libs/syntax/visitor.luau:1019:9-20: (W0) TypeError: Type '(({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*' does not have key 'istoken'

./lute/std/libs/syntax/visitor.luau:1020:3-12: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	nil | "nil" | "false" | "true" | string | "..." | "false" | "true" | "?" | "@checked" | "@deprecated" | "@native"
and at most:
	"nil" & "false" | "true" & string & "..." & "false" | "true" & "?" & "@checked" | "@deprecated" | "@native"
but these types are not compatible with one another.

./lute/std/libs/syntax/visitor.luau:1021:9-24: (W0) TypeError: Type '(((AstExprAnonymousFunction | AstExprBinary | AstExprCall | AstExprGlobal | AstExprGroup | AstExprIfElse | AstExprIndexExpr | AstExprIndexName | AstExprInterpString | AstExprLocal | AstExprTable | AstExprTypeAssertion | AstExprUnary) & { read istoken: false?, kind: "expr" }) | ((AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { read istoken: false?, kind: "type" }) | ((AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { read istoken: false?, kind: "typepack" })... *TRUNCATED*' does not have key 'istableitem'

./lute/std/libs/syntax/visitor.luau:1022:22-25: (W0) TypeError: Expected this to be
	'AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord'
but got
	'(((AstExprAnonymousFunction | AstExprBinary | AstExprCall | AstExprGlobal | AstExprGroup | AstExprIfElse | AstExprIndexExpr | AstExprIndexName | AstExprInterpString | AstExprLocal | AstExprTable | AstExprTypeAssertion | AstExprUnary) & { read istableitem: ~(false?), read istoken: false?, kind: "expr" }) | ((AstTypeArray | AstTypeFunction | AstTypeGroup | AstTypeIntersection | AstTypeReference | AstTypeTable | AstTypeTypeof | AstTypeUnion) & { read istableitem: ~(false?), read istoken: false?, kind: "type" })... *TRUNCATED*'; 
this is because 
	 * in the 1st component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 1st component of the intersection, accessing `kind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` has the negation `"stat"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"stat"` is not a subtype of `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` results in `~"stat"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"stat"` is not a subtype of `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the unionkind` results in `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `kind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the unionkind` results in `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `kind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the unionkind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionkind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionkind` results in `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not exactly `"list"`
	 * in the 1st component of the intersection has the 4th component of the unionistableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 1st component of the intersection has the 4th component of the unionistableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 1st component of the intersection has the 4th component of the unionkind` results in `"local"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"local"` is not exactly `"list"`
	 * in the 2nd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 2nd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 2nd component of the intersection, accessing `kind` has the negation `"local"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"local"` is not a subtype of `"list"`
	 * in the 2nd component of the intersection, accessing `kind` results in `~"local"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"local"` is not a subtype of `"list"`
	 * in the 3rd component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 3rd component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 3rd component of the intersection, accessing `kind` has the negation `"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"type"` is not a subtype of `"list"`
	 * in the 3rd component of the intersection, accessing `kind` results in `~"type"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"type"` is not a subtype of `"list"`
	 * in the 4th component of the intersection, accessing `istableitem` has the negation `false?` and in the 1st component of the union, accessing `istableitem` results in `true`, and `false?` is not a subtype of `true`
	 * in the 4th component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 4th component of the intersection, accessing `kind` has the negation `"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"expr"` is not a subtype of `"list"`
	 * in the 4th component of the intersection, accessing `kind` results in `~"expr"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"expr"` is not a subtype of `"list"`
	 * in the 5th component of the intersection, accessing `istableitem` results in `~(false?)` and in the 1st component of the union, accessing `istableitem` results in `true`, and `~(false?)` is not a subtype of `true`
	 * in the 5th component of the intersection, accessing `kind` has the negation `"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `"typepack"` is not a subtype of `"list"`
	 * in the 5th component of the intersection, accessing `kind` results in `~"typepack"` and in the 1st component of the union, accessing `kind` results in `"list"`, and `~"typepack"` is not a subtype of `"list"`

/Users/vvijay/git/roblox/lute/lute/std/libs/test/failure.luau:18:35-38: (W0) TypeError: Unknown type 'hook'

./lute/std/libs/test/failure.luau:18:35-38: (W0) TypeError: Unknown type 'hook'

/Users/vvijay/git/roblox/lute/lute/std/libs/test/reporter.luau:10:19-32: (W0) TypeError: Unknown type 'assert.asserts'

./lute/std/libs/test/reporter.luau:10:19-32: (W0) TypeError: Unknown type 'assert.asserts'

./tests/batteries/collections/deque.test.luau:9:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:16:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:24:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:32:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:39:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:48:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:55:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:61:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:81:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:86:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

./tests/batteries/collections/deque.test.luau:91:13-16: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

/Users/vvijay/git/roblox/lute/batteries/difftext/myersdiff.luau:76:8-22: (W0) TableOperations: Using '#' on a table without an array part is likely a bug

/Users/vvijay/git/roblox/lute/batteries/difftext/printdiff.luau:70:10-77: (W0) TypeError: We cannot statically check the type of `string.format` when called with a format string that is not statically known.
If you'd like to use an unchecked `string.format` call, you can cast the format string to `any` using `:: any`.

/Users/vvijay/git/roblox/lute/batteries/difftext/printdiff.luau:72:10-73: (W0) TypeError: We cannot statically check the type of `string.format` when called with a format string that is not statically known.
If you'd like to use an unchecked `string.format` call, you can cast the format string to `any` using `:: any`.

/Users/vvijay/git/roblox/lute/batteries/difftext/printdiff.luau:74:10-73: (W0) TypeError: We cannot statically check the type of `string.format` when called with a format string that is not statically known.
If you'd like to use an unchecked `string.format` call, you can cast the format string to `any` using `:: any`.

/Users/vvijay/git/roblox/lute/lute/std/libs/fs.luau:82:3-14: (W0) TypeError: No valid instantiation could be inferred for generic type parameter V. It was expected to be at least:
	{ event: WatchEvent, filename: path | path }
and at most:
	never
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/fs.luau:90:17-28: (W0) TypeError: No valid instantiation could be inferred for generic type parameter V. It was expected to be at least:
	
and at most:
	never
but these types are not compatible with one another.

/Users/vvijay/git/roblox/lute/lute/std/libs/fs.luau:91:11-20: (W0) TypeError: Value of type 'unknown?' could be nil

/Users/vvijay/git/roblox/lute/lute/std/libs/fs.luau:91:11-20: (W0) TypeError: Type 'unknown' does not have key 'event'

/Users/vvijay/git/roblox/lute/lute/std/libs/fs.luau:135:34-39: (W0) TypeError: Expected this to be
	'path | pathdata | string'
but got
	'path | path'; 
this is because 
	 * in the 1st component of the union, the table portion is `pathdata` and in the 3rd component of the union, the table portion is `pathdata`, and `pathdata` is not a subtype of `pathdata`
	 * the 1st component of the union is `path` and the 2nd component of the union is `pathdata`, and `path` is not a subtype of `pathdata`
	 * the 1st component of the union is `path` in the latter type and `string` in the former type, and `path` is not a subtype of `string`

/Users/vvijay/git/roblox/lute/lute/std/libs/fs.luau:184:52-68: (W0) TypeError: Value of type 'walkoptions?' could be nil

/Users/vvijay/git/roblox/lute/lute/std/libs/fs.luau:192:11-17: (W0) TypeError: Expected this to be
	'(path | path)?'
but got
	'path | path | pathdata | pathdata | string'; 
this is because 
	 * the 1st component of the union is `string` and the 2nd component of the union is `path`, and `string` is not a subtype of `path`
	 * the 1st component of the union is `string` and the 3rd component of the union is `nil`, and `string` is not a subtype of `nil`
	 * the 1st component of the union is `string` in the latter type and `path` in the former type, and `string` is not a subtype of `path`
	 * the 3rd component of the union is `pathdata` and the 1st component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 3rd component of the union is `pathdata` and the 2nd component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 3rd component of the union is `pathdata` in the latter type and `nil` in the former type, and `pathdata` is not a subtype of `nil`
	 * the 4th component of the union is `pathdata` and the 1st component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 4th component of the union is `pathdata` and the 2nd component of the union is `path`, and `pathdata` is not a subtype of `path`
	 * the 4th component of the union is `pathdata` and the 3rd component of the union is `nil`, and `pathdata` is not a subtype of `nil`

./tests/cli/check.test.luau:1:7-8: (W0) ImportUnused: Import 'fs' is never used; prefix with '_' to silence

./tests/cli/check.test.luau:4:7-12: (W0) ImportUnused: Import 'system' is never used; prefix with '_' to silence

./tests/cli/test.test.luau:3:7-12: (W0) ImportUnused: Import 'system' is never used; prefix with '_' to silence

./tests/parserExamples/assignment-1.luau:1:1-1: (W0) TypeError: Unknown global 'x'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:3:1-1: (W0) TypeError: Unknown global 'a'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:3:4-4: (W0) TypeError: Unknown global 'b'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:5:43-46: (W0) TypeError: Expected this to be 'number', but got 'boolean'

./tests/parserExamples/assignment-1.luau:5:7-7: (W0) TypeError: Unknown global 'c'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:5:13-13: (W0) TypeError: Unknown global 'f'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:5:16-16: (W0) TypeError: Unknown global 'g'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:5:23-23: (W0) TypeError: Unknown global 'h'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:5:31-31: (W0) TypeError: Unknown global 'k'; consider assigning to it first

./tests/parserExamples/assignment-1.luau:5:38-38: (W0) TypeError: Unknown global 'm'; consider assigning to it first

./tests/parserExamples/attributes-1.luau:2:16-18: (W0) FunctionUnused: Function 'foo' is never used; prefix with '_' to silence

./tests/parserExamples/attributes-1.luau:5:10-12: (W0) FunctionUnused: Function 'bar' is never used; prefix with '_' to silence

./tests/parserExamples/attributes-1.luau:7:7-7: (W0) LocalUnused: Variable 'x' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-1.luau:2:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/function-declaration-1.luau:5:10-12: (W0) TypeError: Type '(...any) -> ()' does not have key 'y'

./tests/parserExamples/function-declaration-2.luau:2:10-10: (W0) FunctionUnused: Function 'x' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-3.luau:1:10-10: (W0) FunctionUnused: Function 'a' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-3.luau:3:10-10: (W0) FunctionUnused: Function 'b' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-3.luau:5:10-10: (W0) FunctionUnused: Function 'c' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-3.luau:7:10-10: (W0) FunctionUnused: Function 'd' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-3.luau:9:10-10: (W0) FunctionUnused: Function 'e' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-4.luau:7:1-24: (W0) TypeError: Not all codepaths in this function return 'number'.

./tests/parserExamples/function-declaration-4.luau:1:10-10: (W0) FunctionUnused: Function 'a' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-4.luau:3:10-10: (W0) FunctionUnused: Function 'b' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-4.luau:5:10-10: (W0) FunctionUnused: Function 'c' is never used; prefix with '_' to silence

./tests/parserExamples/function-declaration-4.luau:7:10-10: (W0) FunctionUnused: Function 'd' is never used; prefix with '_' to silence

./tests/parserExamples/generic-for-loop-1.luau:1:27-30: (W0) TypeError: Unknown global 'list'; consider assigning to it first

./tests/parserExamples/generic-for-loop-1.luau:2:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/generic-for-loop-1.luau:5:27-30: (W0) TypeError: Unknown global 'list'; consider assigning to it first

./tests/parserExamples/generic-for-loop-1.luau:6:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/generic-for-loop-1.luau:9:21-24: (W0) TypeError: Unknown global 'list'; consider assigning to it first

./tests/parserExamples/generic-for-loop-1.luau:10:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/if-expression-1.luau:1:7-7: (W0) LocalUnused: Variable 'x' is never used; prefix with '_' to silence

./tests/parserExamples/if-expression-1.luau:2:7-7: (W0) LocalUnused: Variable 'y' is never used; prefix with '_' to silence

./tests/parserExamples/if-expression-2.luau:3:7-10: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/if-expression-2.luau:5:8-11: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/if-expression-2.luau:6:8-11: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/if-expression-2.luau:2:7-7: (W0) LocalUnused: Variable 'x' is never used; prefix with '_' to silence

./tests/parserExamples/interpolated-string-1.luau:1:7-7: (W0) LocalUnused: Variable 'a' is never used; prefix with '_' to silence

./tests/parserExamples/interpolated-string-1.luau:2:7-7: (W0) LocalUnused: Variable 'b' is never used; prefix with '_' to silence

./tests/parserExamples/interpolated-string-1.luau:3:7-7: (W0) LocalUnused: Variable 'c' is never used; prefix with '_' to silence

./tests/parserExamples/interpolated-string-1.luau:4:7-7: (W0) LocalUnused: Variable 'd' is never used; prefix with '_' to silence

./tests/parserExamples/interpolated-string-2.luau:7:9-15: (W0) TypeError: Unknown global 'subject'; consider assigning to it first

./tests/parserExamples/interpolated-string-2.luau:7:24-30: (W0) TypeError: Unknown global 'subject'; consider assigning to it first

./tests/parserExamples/interpolated-string-2.luau:2:7-7: (W0) LocalUnused: Variable 'x' is never used; prefix with '_' to silence

./tests/parserExamples/local-assignment-1.luau:1:7-7: (W0) LocalUnused: Variable 'a' is never used; prefix with '_' to silence

./tests/parserExamples/local-assignment-1.luau:2:7-7: (W0) LocalUnused: Variable 'b' is never used; prefix with '_' to silence

./tests/parserExamples/local-assignment-1.luau:2:10-10: (W0) LocalUnused: Variable 'c' is never used; prefix with '_' to silence

./tests/parserExamples/local-assignment-1.luau:3:7-7: (W0) LocalUnused: Variable 'd' is never used; prefix with '_' to silence

./tests/parserExamples/local-function-declaration-1.luau:2:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/local-function-declaration-1.luau:1:16-16: (W0) FunctionUnused: Function 'x' is never used; prefix with '_' to silence

./tests/parserExamples/numeric-for-loop-1.luau:2:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/numeric-for-loop-1.luau:4:9-13: (W0) TypeError: Unknown global 'start'; consider assigning to it first

./tests/parserExamples/numeric-for-loop-1.luau:4:16-20: (W0) TypeError: Unknown global 'final'; consider assigning to it first

./tests/parserExamples/repeat-until-1.luau:2:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/parserExamples/repeat-until-1.luau:3:7-15: (W0) TypeError: Unknown global 'condition'; consider assigning to it first

./tests/parserExamples/table-1.luau:1:7-7: (W0) LocalUnused: Variable 'x' is never used; prefix with '_' to silence

./tests/parserExamples/table-2.luau:1:7-7: (W0) LocalUnused: Variable 'x' is never used; prefix with '_' to silence

./tests/parserExamples/type-assertion-1.luau:1:7-7: (W0) LocalUnused: Variable 'x' is never used; prefix with '_' to silence

./tests/parserExamples/while-1.luau:1:7-15: (W0) TypeError: Unknown global 'condition'; consider assigning to it first

./tests/parserExamples/while-1.luau:2:2-5: (W0) TypeError: Unknown global 'call'; consider assigning to it first

./tests/runtime/crypto.test.luau:66:26-30: (W0) TypeError: Expected this to be 'secretbox', but got '{ ciphertext: string, key: buffer, nonce: buffer }'; 
accessing `ciphertext` results in `string` in the latter type and `buffer` in the former type, and `string` is not a subtype of `buffer`

./tests/runtime/crypto.test.luau:108:56-68: (W0) TypeError: Argument count mismatch. Function expects 1 argument, but none are specified

./tests/runtime/crypto.test.luau:112:72-84: (W0) TypeError: Argument count mismatch. Function expects 1 argument, but none are specified

./tests/src/packages/package_aware_require/dep/module.luau:1:16-30: (W0) TypeError: Unknown require: unsupported path

./tests/src/packages/pkgrun_with_lockfile/Packages/dep/src/init.luau:1:16-38: (W0) TypeError: Unknown require: unsupported path

./tests/src/require/config_tests/config_ambiguity/requirer.luau:1:8-22: (W0) TypeError: Unknown require: unsupported path

./tests/src/require/config_tests/config_cannot_be_required/requirer.luau:1:8-27: (W0) TypeError: Unknown require: unsupported path

./tests/src/require/config_tests/tilde_config/main.luau:1:11-31: (W0) TypeError: Unknown require: unsupported path

./tests/src/require/config_tests/tilde_config/main.luau:1:7-7: (W0) ImportUnused: Import 'x' is never used; prefix with '_' to silence

./tests/src/require/without_config/ambiguous_directory_requirer.luau:1:16-58: (W0) TypeError: Unknown require: unsupported path

./tests/src/require/without_config/ambiguous_file_requirer.luau:1:16-53: (W0) TypeError: Unknown require: unsupported path

/Users/vvijay/git/roblox/lute/tests/src/staticrequires/circular_a.luau:2:11-33: (W0) TypeError: Cyclic module dependency: /Users/vvijay/git/roblox/lute/tests/src/staticrequires/circular_b.luau -> /Users/vvijay/git/roblox/lute/tests/src/staticrequires/circular_a.luau

/Users/vvijay/git/roblox/lute/tests/src/staticrequires/circular_b.luau:2:11-33: (W0) TypeError: Cyclic module dependency: /Users/vvijay/git/roblox/lute/tests/src/staticrequires/circular_a.luau -> /Users/vvijay/git/roblox/lute/tests/src/staticrequires/circular_b.luau

./tests/src/staticrequires/main.luau:2:7-13: (W0) ImportUnused: Import 'example' is never used; prefix with '_' to silence

./tests/std/fs.test.luau:114:13-24: (W0) TypeError: Value of type 'WatchEvent?' could be nil

./tests/std/fs.test.luau:133:18-20: (W0) TypeError: Function only returns 1 value, but 2 are required here

./tests/std/fs.test.luau:142:18-20: (W0) TypeError: Function only returns 1 value, but 2 are required here

./tests/std/fs.test.luau:210:18-20: (W0) TypeError: Function only returns 1 value, but 2 are required here

./tests/std/json.test.luau:79:15-21: (W0) TypeError: Value of type '(array | boolean | number | object | string)?' could be nil

./tests/std/json.test.luau:79:15-21: (W0) TypeError: Key 'c' is missing from 'boolean', 'number', 'string', 'array' in the type 'array | boolean | number | object | string'

./tests/std/json.test.luau:80:20-26: (W0) TypeError: Value of type '(array | boolean | number | object | string)?' could be nil

./tests/std/json.test.luau:80:20-26: (W0) TypeError: Key 'd' is missing from 'boolean', 'number', 'string', 'array' in the type 'array | boolean | number | object | string'

./tests/std/json.test.luau:81:17-23: (W0) TypeError: Value of type '(array | boolean | number | object | string)?' could be nil

./tests/std/json.test.luau:81:17-23: (W0) TypeError: Key 'd' is missing from 'boolean', 'number', 'string', 'array' in the type 'array | boolean | number | object | string'

./tests/std/json.test.luau:81:16-23: (W0) TypeError: Operator '#' could not be applied to operand of type (array | boolean | number | object | string)?; there is no corresponding overload for __len

./tests/std/json.test.luau:81:16-23: (W0) TypeError: Value of type '(array | boolean | number | object | string)?' could be nil

./tests/std/json.test.luau:82:21-27: (W0) TypeError: Value of type '(array | boolean | number | object | string)?' could be nil

./tests/std/json.test.luau:82:21-27: (W0) TypeError: Key 'd' is missing from 'boolean', 'number', 'string', 'array' in the type 'array | boolean | number | object | string'

./tests/std/json.test.luau:82:21-30: (W0) TypeError: Value of type '(array | boolean | number | object | string)?' could be nil

./tests/std/process.test.luau:39:18-18: (W0) TypeError: Expected this to be
	'{ [unknown]: unknown }'
but got
	'ProcessResult'

./tests/std/process.test.luau:46:18-18: (W0) TypeError: Expected this to be
	'{ [unknown]: unknown }'
but got
	'ProcessResult'

./tests/std/process.test.luau:55:17-18: (W0) TypeError: Expected this to be
	'{ [unknown]: unknown }'
but got
	'ProcessResult'

./tests/std/process.test.luau:65:18-18: (W0) TypeError: Expected this to be
	'{ [unknown]: unknown }'
but got
	'ProcessResult'

./tests/std/process.test.luau:79:45-46: (W0) TypeError: Expected this to be 'string?', but got '{  }'; 
this is because 
	 * the 1st component of the union is `string`, and `{  }` is not a subtype of `string`
	 * the 2nd component of the union is `nil`, and `{  }` is not a subtype of `nil`

./tests/std/process.test.luau:83:47-48: (W0) TypeError: Expected this to be '("default" | "inherit" | "none")?', but got '{  }'; 
this is because 
	 * the 1st component of the union is `"default"`, and `{  }` is not a subtype of `"default"`
	 * the 2nd component of the union is `"inherit"`, and `{  }` is not a subtype of `"inherit"`
	 * the 3rd component of the union is `"none"`, and `{  }` is not a subtype of `"none"`
	 * the 4th component of the union is `nil`, and `{  }` is not a subtype of `nil`

./tests/std/process.test.luau:87:45-57: (W0) TypeError: Expected this to be '{ [string]: string }?', but got 'string'; 
this is because 
	 * in the metatable portion, accessing `__index` results in `typeof(string)` and the 1st component of the union is `{ [string]: string }`, and `typeof(string)` is not a subtype of `{ [string]: string }`
	 * the 2nd component of the union is `nil`, and `string` is not a subtype of `nil`

./tests/std/process.test.luau:91:37-52: (W0) TypeError: Expected this to be 'ProcessRunOptions?', but got 'string'; 
this is because 
	 * in the metatable portion, accessing `__index` results in `typeof(string)` and the 1st component of the union is `ProcessRunOptions`, and `typeof(string)` is not a subtype of `ProcessRunOptions`
	 * the 2nd component of the union is `nil`, and `string` is not a subtype of `nil`

./tests/std/process.test.luau:95:37-49: (W0) TypeError: Expected this to be 'ProcessRunOptions?', but got 'string'; 
this is because 
	 * in the metatable portion, accessing `__index` results in `typeof(string)` and the 1st component of the union is `ProcessRunOptions`, and `typeof(string)` is not a subtype of `ProcessRunOptions`
	 * the 2nd component of the union is `nil`, and `string` is not a subtype of `nil`

./tests/std/process.test.luau:111:4-17: (W0) TypeError: Argument count mismatch. Function expects 1 to 2 arguments, but none are specified

./tests/std/process.test.luau:115:19-20: (W0) TypeError: Expected this to be 'string', but got '{  }'

./tests/std/process.test.luau:119:44-45: (W0) TypeError: Expected this to be 'string?', but got '{  }'; 
this is because 
	 * the 1st component of the union is `string`, and `{  }` is not a subtype of `string`
	 * the 2nd component of the union is `nil`, and `{  }` is not a subtype of `nil`

./tests/std/process.test.luau:123:41-42: (W0) TypeError: Expected this to be 'string?', but got '{  }'; 
this is because 
	 * the 1st component of the union is `string`, and `{  }` is not a subtype of `string`
	 * the 2nd component of the union is `nil`, and `{  }` is not a subtype of `nil`

./tests/std/process.test.luau:126:43-44: (W0) TypeError: Expected this to be '("default" | "inherit" | "none")?', but got '{  }'; 
this is because 
	 * the 1st component of the union is `"default"`, and `{  }` is not a subtype of `"default"`
	 * the 2nd component of the union is `"inherit"`, and `{  }` is not a subtype of `"inherit"`
	 * the 3rd component of the union is `"none"`, and `{  }` is not a subtype of `"none"`
	 * the 4th component of the union is `nil`, and `{  }` is not a subtype of `nil`

./tests/std/process.test.luau:130:41-53: (W0) TypeError: Expected this to be '{ [string]: string }?', but got 'string'; 
this is because 
	 * in the metatable portion, accessing `__index` results in `typeof(string)` and the 1st component of the union is `{ [string]: string }`, and `typeof(string)` is not a subtype of `{ [string]: string }`
	 * the 2nd component of the union is `nil`, and `string` is not a subtype of `nil`

./tests/std/process.test.luau:134:33-48: (W0) TypeError: Expected this to be 'ProcessSystemOptions?', but got 'string'; 
this is because 
	 * in the metatable portion, accessing `__index` results in `typeof(string)` and the 1st component of the union is `ProcessSystemOptions`, and `typeof(string)` is not a subtype of `ProcessSystemOptions`
	 * the 2nd component of the union is `nil`, and `string` is not a subtype of `nil`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/printer.luau:41:23-27: (W0) TypeError: Expected this to be
	'{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'Token<string>'; 
this is because 
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"`, and `string` is not exactly `"nil"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."`, and `string` is not exactly `"..."`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"`, and `string` is not exactly `"break"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"`, and `string` is not exactly `"continue"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"`, and `string` is not exactly `"?"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"@checked"`, and `string` is not exactly `"@checked"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"@native"`, and `string` is not exactly `"@native"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 3rd component of the union as `"@deprecated"`, and `string` is not exactly `"@deprecated"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `Token<string>` is not a subtype of `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `Token<string>` is not a subtype of `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `Token<string>` is not a subtype of `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `Token<string>` is not a subtype of `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `Token<string>` is not a subtype of `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `Token<string>` is not a subtype of `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `Token<string>` is not a subtype of `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `Token<string>` is not a subtype of `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `Token<string>` is not a subtype of `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `Token<string>` is not a subtype of `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `Token<string>` is not a subtype of `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `Token<string>` is not a subtype of `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `Token<string>` is not a subtype of `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `Token<string>` is not a subtype of `AstExprCall`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `Token<string>` is not a subtype of `{ kind: "stat" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `Token<string>` is not a subtype of `AstStatLocal`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `Token<string>` is not a subtype of `AstStatFor`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `Token<string>` is not a subtype of `AstStatForIn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `Token<string>` is not a subtype of `AstStatAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `Token<string>` is not a subtype of `AstStatCompoundAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `Token<string>` is not a subtype of `AstStatFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `Token<string>` is not a subtype of `AstStatLocalFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `Token<string>` is not a subtype of `AstStatTypeAlias`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `Token<string>` is not a subtype of `AstStatTypeFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `Token<string>` is not a subtype of `AstStatBlock`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `Token<string>` is not a subtype of `AstStatDo`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `Token<string>` is not a subtype of `AstStatIf`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `Token<string>` is not a subtype of `AstStatWhile`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `Token<string>` is not a subtype of `AstStatRepeat`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `Token<string>` is not a subtype of `AstStatReturn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `Token<string>` is not a subtype of `AstStatExpr`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `Token<string>` is not a subtype of `{ kind: "type" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `Token<string>` is not a subtype of `AstTypeTable`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `Token<string>` is not a subtype of `AstTypeFunction`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `Token<string>` is not a subtype of `AstTypeReference`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `Token<string>` is not a subtype of `AstTypeTypeof`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `Token<string>` is not a subtype of `AstTypeGroup`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `Token<string>` is not a subtype of `AstTypeUnion`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `Token<string>` is not a subtype of `AstTypeIntersection`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `Token<string>` is not a subtype of `AstTypeArray`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `Token<string>` is not a subtype of `{ kind: "typepack" }`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `Token<string>` is not a subtype of `AstTypePackExplicit`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `Token<string>` is not a subtype of `AstTypePackGeneric`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `Token<string>` is not a subtype of `AstTypePackVariadic`
	 * in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `Token<string>` is not a subtype of `AstLocal`
	 * in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `Token<string>` is not a subtype of `{ kind: "attribute", location: span }`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/printer.luau:42:50-54: (W0) TypeError: Expected this to be
	'{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'Token<string>'; 
this is because 
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"`, and `string` is not exactly `"nil"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."`, and `string` is not exactly `"..."`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"`, and `string` is not exactly `"break"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"`, and `string` is not exactly `"continue"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"`, and `string` is not exactly `"?"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"@checked"`, and `string` is not exactly `"@checked"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"@native"`, and `string` is not exactly `"@native"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 3rd component of the union as `"@deprecated"`, and `string` is not exactly `"@deprecated"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `Token<string>` is not a subtype of `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `Token<string>` is not a subtype of `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `Token<string>` is not a subtype of `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `Token<string>` is not a subtype of `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `Token<string>` is not a subtype of `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `Token<string>` is not a subtype of `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `Token<string>` is not a subtype of `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `Token<string>` is not a subtype of `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `Token<string>` is not a subtype of `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `Token<string>` is not a subtype of `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `Token<string>` is not a subtype of `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `Token<string>` is not a subtype of `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `Token<string>` is not a subtype of `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `Token<string>` is not a subtype of `AstExprCall`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `Token<string>` is not a subtype of `{ kind: "stat" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `Token<string>` is not a subtype of `AstStatLocal`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `Token<string>` is not a subtype of `AstStatFor`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `Token<string>` is not a subtype of `AstStatForIn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `Token<string>` is not a subtype of `AstStatAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `Token<string>` is not a subtype of `AstStatCompoundAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `Token<string>` is not a subtype of `AstStatFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `Token<string>` is not a subtype of `AstStatLocalFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `Token<string>` is not a subtype of `AstStatTypeAlias`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `Token<string>` is not a subtype of `AstStatTypeFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `Token<string>` is not a subtype of `AstStatBlock`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `Token<string>` is not a subtype of `AstStatDo`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `Token<string>` is not a subtype of `AstStatIf`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `Token<string>` is not a subtype of `AstStatWhile`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `Token<string>` is not a subtype of `AstStatRepeat`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `Token<string>` is not a subtype of `AstStatReturn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `Token<string>` is not a subtype of `AstStatExpr`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `Token<string>` is not a subtype of `{ kind: "type" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `Token<string>` is not a subtype of `AstTypeTable`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `Token<string>` is not a subtype of `AstTypeFunction`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `Token<string>` is not a subtype of `AstTypeReference`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `Token<string>` is not a subtype of `AstTypeTypeof`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `Token<string>` is not a subtype of `AstTypeGroup`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `Token<string>` is not a subtype of `AstTypeUnion`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `Token<string>` is not a subtype of `AstTypeIntersection`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `Token<string>` is not a subtype of `AstTypeArray`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `Token<string>` is not a subtype of `{ kind: "typepack" }`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `Token<string>` is not a subtype of `AstTypePackExplicit`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `Token<string>` is not a subtype of `AstTypePackGeneric`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `Token<string>` is not a subtype of `AstTypePackVariadic`
	 * in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `Token<string>` is not a subtype of `AstLocal`
	 * in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `Token<string>` is not a subtype of `{ kind: "attribute", location: span }`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/printer.luau:42:25-29: (W0) TypeError: Expected this to be
	'{ location: span } & (({ kind: "expr" } & (AstExprGroup | (Token<"nil"> & { kind: "expr", location: span, tag: "nil" }) | (Token<"false" | "true"> & { kind: "expr", location: span, tag: "boolean", value: boolean }) | (Token<string> & { kind: "expr", location: span, tag: "number", value: number }) | (Token<string> & { blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }) | AstExprLocal | AstExprGlobal | (Token<"..."> & { kind: "expr", location: span, tag: "vararg" }) | AstExprCall... *TRUNCATED*'
but got
	'Token<string>'; 
this is because 
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` results in `"nil"`, and `string` is not exactly `"nil"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"..."`, and `string` is not exactly `"..."`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` results in `"break"`, and `string` is not exactly `"break"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 1st component of the intersection, accessing `text` results in `"continue"`, and `string` is not exactly `"continue"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"true"`, and `string` is not exactly `"true"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"false"`, and `string` is not exactly `"false"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 1st component of the intersection, accessing `text` results in `"?"`, and `string` is not exactly `"?"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 1st component of the union as `"@checked"`, and `string` is not exactly `"@checked"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 2nd component of the union as `"@native"`, and `string` is not exactly `"@native"`
	 * accessing `text` results in `string` and in the 2nd component of the intersection has the 6th component of the unionthe 1st component of the intersection, accessing `text` has the 3rd component of the union as `"@deprecated"`, and `string` is not exactly `"@deprecated"`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 1st component of the intersection is `{ kind: "expr" }`, and `Token<string>` is not a subtype of `{ kind: "expr" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstExprIndexName`, and `Token<string>` is not a subtype of `AstExprIndexName`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstExprIndexExpr`, and `Token<string>` is not a subtype of `AstExprIndexExpr`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstExprAnonymousFunction`, and `Token<string>` is not a subtype of `AstExprAnonymousFunction`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstExprTable`, and `Token<string>` is not a subtype of `AstExprTable`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstExprUnary`, and `Token<string>` is not a subtype of `AstExprUnary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstExprBinary`, and `Token<string>` is not a subtype of `AstExprBinary`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstExprInterpString`, and `Token<string>` is not a subtype of `AstExprInterpString`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstExprTypeAssertion`, and `Token<string>` is not a subtype of `AstExprTypeAssertion`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstExprIfElse`, and `Token<string>` is not a subtype of `AstExprIfElse`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstExprGroup`, and `Token<string>` is not a subtype of `AstExprGroup`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "nil" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "nil" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "number", value: number }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "number", value: number }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 5th component of the unionthe 2nd component of the intersection is `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstExprLocal`, and `Token<string>` is not a subtype of `AstExprLocal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstExprGlobal`, and `Token<string>` is not a subtype of `AstExprGlobal`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "expr", location: span, tag: "vararg" }`, and `Token<string>` is not a subtype of `{ kind: "expr", location: span, tag: "vararg" }`
	 * in the 2nd component of the intersection has the 1st component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstExprCall`, and `Token<string>` is not a subtype of `AstExprCall`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 1st component of the intersection is `{ kind: "stat" }`, and `Token<string>` is not a subtype of `{ kind: "stat" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstStatLocal`, and `Token<string>` is not a subtype of `AstStatLocal`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstStatFor`, and `Token<string>` is not a subtype of `AstStatFor`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 12th component of the union as `AstStatForIn`, and `Token<string>` is not a subtype of `AstStatForIn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 13th component of the union as `AstStatAssign`, and `Token<string>` is not a subtype of `AstStatAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 14th component of the union as `AstStatCompoundAssign`, and `Token<string>` is not a subtype of `AstStatCompoundAssign`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 15th component of the union as `AstStatFunction`, and `Token<string>` is not a subtype of `AstStatFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 16th component of the union as `AstStatLocalFunction`, and `Token<string>` is not a subtype of `AstStatLocalFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 17th component of the union as `AstStatTypeAlias`, and `Token<string>` is not a subtype of `AstStatTypeAlias`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 18th component of the union as `AstStatTypeFunction`, and `Token<string>` is not a subtype of `AstStatTypeFunction`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstStatBlock`, and `Token<string>` is not a subtype of `AstStatBlock`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstStatDo`, and `Token<string>` is not a subtype of `AstStatDo`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstStatIf`, and `Token<string>` is not a subtype of `AstStatIf`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstStatWhile`, and `Token<string>` is not a subtype of `AstStatWhile`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstStatRepeat`, and `Token<string>` is not a subtype of `AstStatRepeat`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "break" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "break" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 7th component of the unionthe 2nd component of the intersection is `{ kind: "stat", location: span, tag: "continue" }`, and `Token<string>` is not a subtype of `{ kind: "stat", location: span, tag: "continue" }`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 8th component of the union as `AstStatReturn`, and `Token<string>` is not a subtype of `AstStatReturn`
	 * in the 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstStatExpr`, and `Token<string>` is not a subtype of `AstStatExpr`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 1st component of the intersection is `{ kind: "type" }`, and `Token<string>` is not a subtype of `{ kind: "type" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 10th component of the union as `AstTypeTable`, and `Token<string>` is not a subtype of `AstTypeTable`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 11th component of the union as `AstTypeFunction`, and `Token<string>` is not a subtype of `AstTypeFunction`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypeReference`, and `Token<string>` is not a subtype of `AstTypeReference`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 2nd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "boolean", value: boolean }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "boolean", value: boolean }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 4th component of the union as `AstTypeTypeof`, and `Token<string>` is not a subtype of `AstTypeTypeof`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 5th component of the union as `AstTypeGroup`, and `Token<string>` is not a subtype of `AstTypeGroup`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 6th component of the union as `AstTypeUnion`, and `Token<string>` is not a subtype of `AstTypeUnion`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 7th component of the union as `AstTypeIntersection`, and `Token<string>` is not a subtype of `AstTypeIntersection`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 8th component of the unionthe 2nd component of the intersection is `{ kind: "type", location: span, tag: "optional" }`, and `Token<string>` is not a subtype of `{ kind: "type", location: span, tag: "optional" }`
	 * in the 2nd component of the intersection has the 3rd component of the unionthe 2nd component of the intersection has the 9th component of the union as `AstTypeArray`, and `Token<string>` is not a subtype of `AstTypeArray`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 1st component of the intersection is `{ kind: "typepack" }`, and `Token<string>` is not a subtype of `{ kind: "typepack" }`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 1st component of the union as `AstTypePackExplicit`, and `Token<string>` is not a subtype of `AstTypePackExplicit`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 2nd component of the union as `AstTypePackGeneric`, and `Token<string>` is not a subtype of `AstTypePackGeneric`
	 * in the 2nd component of the intersection has the 4th component of the unionthe 2nd component of the intersection has the 3rd component of the union as `AstTypePackVariadic`, and `Token<string>` is not a subtype of `AstTypePackVariadic`
	 * in the 2nd component of the intersection has the 5th component of the union as `AstLocal`, and `Token<string>` is not a subtype of `AstLocal`
	 * in the 2nd component of the intersection has the 6th component of the unionthe 2nd component of the intersection is `{ kind: "attribute", location: span }`, and `Token<string>` is not a subtype of `{ kind: "attribute", location: span }`

/Users/vvijay/git/roblox/lute/lute/std/libs/syntax/printer.luau:203:2-19: (W0) TypeError: No valid instantiation could be inferred for generic type parameter T. It was expected to be at least:
	"" | nil
and at most:
	""
but these types are not compatible with one another.

./tests/std/syntax/parser.test.luau:11:10-24: (W0) TypeError: Unknown type 'asserts.asserts'

./tests/std/syntax/parser.test.luau:12:10-15: (W0) TypeError: Unknown type 'T.span'

./tests/std/syntax/parser.test.luau:36:24-29: (W0) TypeError: Expected this to be 'a, number', but got 'T, T, string?'

./tests/std/syntax/parser.test.luau:50:24-29: (W0) TypeError: Expected this to be 'a, number', but got 'T, T, string?'

./tests/std/syntax/parser.test.luau:64:24-29: (W0) TypeError: Expected this to be 'a, number', but got 'T, T, string?'

./tests/std/syntax/parser.test.luau:67:24-29: (W0) TypeError: Expected this to be 'a, number', but got 'T, T, string?'

./tests/std/syntax/parser.test.luau:81:24-29: (W0) TypeError: Expected this to be 'a, number', but got 'T, T, string?'

./tests/std/syntax/parser.test.luau:84:24-29: (W0) TypeError: Expected this to be 'a, number', but got 'T, T, string?'

./tests/std/syntax/parser.test.luau:531:48-62: (W0) TypeError: Unknown type 'asserts.asserts'

./tests/std/syntax/printer.test.luau:23:10-26: (W0) TypeError: Unknown type 'assertLib.asserts'

./tests/std/syntax/printer.test.luau:45:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:59:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:73:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:92:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:107:7-32: (W0) TypeError: Expected this to be '()', but got '(AstTypePackExplicit | AstTypePackGeneric | AstTypePackVariadic) & { kind: "typepack" }'

./tests/std/syntax/printer.test.luau:105:10-100: (W0) TypeError: Expected this to be 'U?', but got '()'

./tests/std/syntax/printer.test.luau:113:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:129:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:141:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:153:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:165:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:177:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:190:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:202:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:214:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:226:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:238:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:250:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:262:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:274:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:286:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:298:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:310:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:322:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:334:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:346:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:359:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:376:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:391:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:406:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:421:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:436:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:449:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:462:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:475:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:490:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:505:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:519:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:533:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:548:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:563:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:576:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:589:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:603:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:616:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:629:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:642:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:655:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:668:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:681:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:694:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:707:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:720:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:733:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:746:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:759:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:772:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:785:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:798:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:811:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:824:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "expr" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ location: span }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "expr", location: span, tag: "boolean", value: boolean }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "expr", location: span, tag: "number", value: number }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatWhile'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be 'AstStatIf', but got 'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatBlock'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "stat" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprGlobal'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprCall'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "expr", location: span, tag: "vararg" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprGroup'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprIndexExpr'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "stat", location: span, tag: "continue" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ blockdepth: number, kind: "expr", location: span, quotestyle: "block" | "double" | "interp" | "single", tag: "string" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeIntersection'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "expr", location: span, tag: "nil" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatForIn'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprIndexName'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprLocal'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprAnonymousFunction'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprBinary'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatReturn'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatExpr'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatLocal'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatRepeat'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprIfElse'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprUnary'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprTable'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprInterpString'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatLocalFunction'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatTypeAlias'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "stat", location: span, tag: "break" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatAssign'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatCompoundAssign'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatFunction'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "type", location: span, quotestyle: "double" | "single", tag: "string" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be 'AstStatDo', but got 'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprTypeAssertion'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeReference'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be 'AstLocal', but got 'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "type", location: span, tag: "optional" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeUnion'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeGroup'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstStatTypeFunction'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be 'AstStatFor', but got 'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'Token<"nil">'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "typepack" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'Token<"...">'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'Token<string>'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeFunction'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeTypeof'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeArray'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "type" }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ location: span }'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypeTable'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'Token<string>'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'Token<"false" | "true">'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypePackExplicit'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstExprGroup'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypePackVariadic'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'AstTypePackGeneric'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'Token<"false" | "true">'
but got
	'{ kind: "attribute", location: span }'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be
	'{ kind: "type", location: span, tag: "boolean", value: boolean }'
but got
	'Token<"@checked" | "@deprecated" | "@native">'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"nil"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"..."', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"false"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"true"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"false"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"..."', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"nil"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"nil"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"break"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"?"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"continue"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"false"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"false"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"true"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"?"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"true"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"continue"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"true"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"false"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"true"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"false"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"true"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"break"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"..."', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"break"', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"?"', but got '"@native"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly 'string', but got '"@checked"'

./tests/std/syntax/printer.test.luau:835:38-60: (W0) TypeError: Expected this to be exactly '"continue"', but got '"@deprecated"'

./tests/std/syntax/printer.test.luau:838:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:859:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:887:23-31: (W0) TypeError: Key 'key' is missing from 'AstExprTableItemList' in the type 'AstExprTableItemGeneral | AstExprTableItemList | AstExprTableItemRecord'

./tests/std/syntax/printer.test.luau:894:8-13: (W0) TypeError: Expected this to be '{ read eq: (string, string) -> (...unknown) }', but got 'asserts'; 
accessing `eq`, takes `T, T, string?` in the latter type and `string, string` in the former type, and `T, T, string?` is not a supertype of `string, string`

./tests/std/syntax/printer.test.luau:9:16-29: (W0) FunctionUnused: Function 'compareStrings' is never used; prefix with '_' to silence

./tests/std/tableext.test.luau:57:18-18: (W0) TypeError: Expected this to be '{ [unknown]: unknown }', but got '{number}'; 
this is because 
	 * the index type is `number` in the latter type and `unknown` in the former type, and `number` is not exactly `unknown`
	 * the result of indexing is `number` in the latter type and `unknown` in the former type, and `number` is not exactly `unknown`

./tests/std/tableext.test.luau:62:18-18: (W0) TypeError: Expected this to be '{ [unknown]: unknown }', but got '{number}'; 
this is because 
	 * the index type is `number` in the latter type and `unknown` in the former type, and `number` is not exactly `unknown`
	 * the result of indexing is `number` in the latter type and `unknown` in the former type, and `number` is not exactly `unknown`

./tests/std/tableext.test.luau:63:18-18: (W0) TypeError: Expected this to be
	'{ [unknown]: unknown }'
but got
	'{  } | {number}'; 
this is because 
	 * in the 1st component of the union, the index type is `number` and the index type is `unknown`, and `number` is not exactly `unknown`
	 * in the 1st component of the union, the result of indexing is `number` and the result of indexing is `unknown`, and `number` is not exactly `unknown`
	 * the 2nd component of the union is `{  }`, which is not a subtype of `{ [unknown]: unknown }`

./tests/std/test.test.luau:15:10-27: (W0) TypeError: Unknown type 'assertions.asserts'

./tests/std/test.test.luau:113:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:132:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:149:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:168:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:185:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:204:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:223:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:246:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:263:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:282:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:299:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:318:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/std/test.test.luau:337:4-9: (W0) TypeError: Expected this to be '{ read eq: (number, number) -> (...unknown), read strcontains: (string, string) -> (...unknown) }', but got 'asserts'; 
this is because 
	 * accessing `eq`, takes `T, T, string?` in the latter type and `number, number` in the former type, and `T, T, string?` is not a supertype of `number, number`
	 * accessing `strcontains`, takes `string, string, string?, number?` in the latter type and `string, string` in the former type, and `string, string, string?, number?` is not a supertype of `string, string`

./tests/src/require/config_tests/config_ambiguity/.luaurc: Both .luaurc and .config.luau files exist

