# time

```luau
local time = require("@lute/time")
```

## time.duration

```luau
{ seconds: (number) -> ({}), minutes: (number) -> ({}), nanoseconds: (number) -> ({}), weeks: (number) -> ({}), microseconds: (number) -> ({}), hours: (number) -> ({}), days: (number) -> ({}), milliseconds: (number) -> ({}) }
```

## time.now

```luau
() -> ({})
```

## time.since

```luau
({}) -> (number)
```
